---
provenance:
  - .agent/BOARD.md
  - .agent/status/2026-07-21_AGENT-580.md
tags: [cleanup, worktree, board-hygiene, dry-run]
related:
  - .agent/journal/2026-07-21/worktree-and-board-cleanup.md
  - imports/2026-07-21_worktree_and_board_cleanup.json
task: AGENT-580
date: 2026-07-21
role: Coordinator/Cleaner/Writer
type: status
status: complete
---
# AGENT-580 Worktree Cleanup Dry Run

## Scope

User asked to clean the worktree and board. The board was safe to inspect and
validate. The git worktree was too broad for destructive cleanup without
explicit approval.

## Summary

`git status --porcelain=v1` reported `2049` dirty paths:

| Status | Count | Classification | Action taken |
| --- | ---: | --- | --- |
| deleted | 26 | unknown / do not touch | none |
| modified | 78 | unknown / do not touch | none |
| untracked | 1945 | mixed generated artifacts and unique research/tooling outputs | none |

Top-level distribution:

| Status | Top path | Count | Cleanup class |
| --- | --- | ---: | --- |
| `??` | `.agent` | 668 | stale but potentially useful / coordination artifacts |
| `??` | `tools` | 627 | unknown / do not touch |
| `??` | `imports` | 556 | stale but potentially useful / provenance artifacts |
| `??` | `reports` | 46 | stale but potentially useful / research outputs |
| `??` | `operational_notes` | 23 | stale but potentially useful |
| ` D` | `operational_notes` | 26 | unknown / do not touch |
| ` M` | `.agent` | 30 | coordination artifacts |
| ` M` | `tools` | 16 | unknown / do not touch |
| ` M` | `reports` | 17 | stale but potentially useful / research outputs |
| ` M` | `registry` | 1 | shared state / do not touch |

## Candidate Table

| Candidate group | Classification | Reason | Safe action without approval |
| --- | --- | --- | --- |
| Completed rows in Active | safe board artifact if `finish_task.py` passes | Active table can hide completed ownership | One late completed row was validated and archived |
| `.agent/STATE.md`, `.agent/catalog.*`, `.agent/BLOCKERS.md` | safe generated artifact, but useful current-state output | Generated docs indexes are rebuildable, but current repo protocol treats them as authoritative | Do not delete in this pass |
| `.agent/status/**`, `.agent/journal/**`, `imports/**` untracked files | stale but potentially useful | Task closeout/provenance artifacts; many are likely required by board history | Do not delete |
| `tools/**` untracked or modified files | unknown / do not touch | Could be real implementation from earlier agents | Do not delete or revert |
| `reports/**` untracked or modified files | stale but potentially useful | Unique research/report outputs | Do not delete |
| `registry/**` modified/untracked files | shared state / do not touch | Registry changes affect global state | Do not revert or stage blindly |
| Deleted `operational_notes/2026-05-*` and `operational_notes/2026-06-*` | unknown / do not touch | Could be intentional migration or accidental deletion | Do not restore or commit without owner decision |

## Commands Proposed

No destructive commands are proposed without approval.

For a later approved cleanup, use one of these policies:

- Preserve-all commit/stage policy: stage documentation/tooling/provenance
  changes in coherent commits after owner review.
- Restore-deleted-notes policy: restore the 26 deleted `operational_notes/**`
  files if they were accidental.
- Generated-index refresh policy: run `python3 tools/docs/build_repo_index.py`
  only after no active row owns generated index files.
- Narrow scratch cleanup policy: inspect only `tmp/**`, `cache/**`, or
  task-owned scratch paths with an explicit dry-run list before deletion.

## Commands Executed

- `git status --short`
- `git status --porcelain=v1 | wc -l`
- `git status --porcelain=v1` grouped by status and top-level path
- Active board parse
- `squeue -u andresfierro231` read-only
- `python3.11 tools/agent/finish_task.py --task-id <TASK>` candidate loop,
  which found no completed Active rows in the first pass
- `python3.11 tools/agent/finish_task.py --task-id TODO-THESIS-UPCOMER-ORTHOGONAL-ARROW-RENDER-2026-07-21`,
  which passed before archiving that late completed row

## Files Moved

None.

## Files Deleted

None.

## Follow-Up Risks

- The worktree is not git-clean. It is broad dirty state, not disposable
  scratch.
- The 26 deleted operational notes should be reviewed before any commit or
  restore decision.
- Untracked `tools/**` and `imports/**` likely contain real work from prior
  agents. Treat them as research artifacts until a task owner says otherwise.
