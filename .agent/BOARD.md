## Active
| Task ID | Role | Owner | Scope | Goal |
| --- | --- | --- | --- | --- |
| TODO-EXPERIMENT-BASIS-AMBIENT-HEAT-LOSS-FRICTION-MODEL-FORM-SEARCH-2026-07-23 | Forward-pred / Hydraulics / Thermal-modeling / Implementer / Tester / Writer / Reviewer | claude / complete 2026-07-23 | `.agent/BOARD.md` (own row only), `.agent/status/2026-07-23_TODO-EXPERIMENT-BASIS-AMBIENT-HEAT-LOSS-FRICTION-MODEL-FORM-SEARCH-2026-07-23.md`, `.agent/journal/2026-07-23/experiment-basis-ambient-heat-loss-friction-model-form-search.md`, `imports/2026-07-23_experiment_basis_ambient_heat_loss_friction_model_form_search.json`, `operational_notes/07-26/23/2026-07-23_EXPERIMENT_BASIS_AMBIENT_HEAT_LOSS_FRICTION_MODEL_FORM_SEARCH.md`, `tools/analyze/build_experiment_basis_ambient_heat_loss_friction_model_form_search.py`, `tools/analyze/test_experiment_basis_ambient_heat_loss_friction_model_form_search.py`, `work_products/2026-07/2026-07-23/2026-07-23_experiment_basis_ambient_heat_loss_friction_model_form_search/**`. READ-ONLY: `2026-07-23_empirical_bias_salt1_4_refit_holdout_external_score/**`, `2026-07-23_combined_best_current_model_temp_mdot_correction/**` (own prior packages, now off-scope-labeled for CFD main body, not modified further here), `2026-07-23_salt2_pm5_holdout_inputs_and_f2_score/**` / `2026-07-23_f2_empirical_holdout_freeze_and_score/**` / `2026-07-23_cfd_basis_valsalt2_...` (the separate CFD-basis main-body track, do not touch), `2026-07-20_tswfc2_bounded_nominal_scorecard/case_outputs/**`, `../cfd-modeling-tools/tamu_first_order_model/Fluid/**` (read/run only, includes `friction_closures.py`, `profile_descriptor_closure.py`, `results/diagnostics/joint_htc_friction_calibration_weekend_focused_v1/**`, `results/diagnostics/practical_reduced_order_broadened_v1/**` cited as prior undocumented sweeps, no edits), canonical split policy, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, thesis current/LaTeX files, blocker register source, generated docs index files, and all other paths. No native-output mutation, registry/admission mutation, scheduler action, solver/postprocessing/sampler/harvest launch, Fluid/external edit (read/run the existing solver only), thesis current/LaTeX edit, physical source/property/Qwall release, physical candidate freeze, physical-closure claim (insulation~8%-of-nameplate is a calibrated hypothesis, NOT independently verified against the rig), coefficient admission as physically confirmed, S11/S15/S6 trigger, blocker-register source change before closeout, generated-index refresh before closeout, deletion, staging, commit, or push. This entire package is validated against EXPERIMENTAL measured_K (validation_table.csv), explicitly NOT the CFD-only thesis main body (which uses CFD reference_k, tracked separately in the concurrent CFD-basis packages) — must carry an explicit off-scope-for-CFD-main-body banner exactly like the two prior packages it extends. This is also a further (3rd+) exposure of salt2_lo5q/salt2_hi5q/val_salt2 within this session. | User asked to find the best model form for reducing TP/TW error via real solver-level model-form search (not just post-hoc empirical correction), using all available compute (256-core SLURM allocation) for parallel solver sweeps. Found: (1) friction closure comparison on the TSWFC2 candidate — F3_hagenbach wins mdot (mean 11.8% vs F1's 32.6%, zero fitting) with T-correction unchanged (confirms mdot/T decoupling); (2) discovered two undocumented prior sweeps in the Fluid repo (`joint_htc_friction_calibration_weekend_focused_v1`, `practical_reduced_order_broadened_v1`) showing the friction/HTC-shape space is exhausted (TP/TW stuck 65-68K); (3) found and corrected a sign error in `outer_insulation_multiplier` (scales thickness UP, not down) and `major_loss_multiplier` compensation direction via direct empirical test; (4) found a genuine physical optimum near insulation=8% of nameplate thickness + major_loss_multiplier=0.6, giving mean TP=5.73K/TW=20.69K/mdot=4.5% across ALL FOUR Salt1-4 train cases and MAE 5.39/2.40/7.42K on real validation data with ZERO fitted parameters; (5) at user's explicit request, tested whether internal-Nu (up to 500x, insulation held at reported/nameplate) could instead explain the gap — decisively ruled out (asymptotes at TP=94.1K, only 2.4K of ~96K gap closeable), which is quantitative evidence FOR the project's standing guardrail against tuning Nu to absorb heat-loss residuals, not just deference to it. Per user decision, this remains the experiment-anchored (measured_K) track, explicitly not redone against CFD reference_k, and not conflated with the separate CFD-basis main-body track. Package complete: `work_products/2026-07/2026-07-23/2026-07-23_experiment_basis_ambient_heat_loss_friction_model_form_search/` (16 CSVs + README + summary.json), `tools/analyze/build_experiment_basis_ambient_heat_loss_friction_model_form_search.py` + test script (13 checks, all pass), status file, journal entry, imports manifest, and operational note all written; every independently-checkable number (F1-based F2 coefficients, M3 comparator, internal-Nu guardrail phrase, F4/F3_hagenbach citations, both prior sweeps' TP/TW-stuck numbers) verified against its source file; session-only ad hoc solve_case() numbers cited with explicit provenance, not re-run. STATUS: COMPLETE 2026-07-23. |
| TODO-CFD-BASIS-VALSALT2-EXTERNAL-SCORE-AND-EXP-BASIS-RELABEL-2026-07-23 | Forward-pred / cfd-pp / Writer / Implementer / Tester / Reviewer | claude / in progress 2026-07-23 | `.agent/BOARD.md` (own row only), `.agent/status/2026-07-23_TODO-CFD-BASIS-VALSALT2-EXTERNAL-SCORE-AND-EXP-BASIS-RELABEL-2026-07-23.md`, `.agent/journal/2026-07-23/cfd-basis-valsalt2-external-score-and-exp-basis-relabel.md`, `imports/2026-07-23_cfd_basis_valsalt2_external_score_and_exp_basis_relabel.json`, `tools/analyze/build_salt2_pm5_holdout_inputs_and_f2_score.py` (extend for val_salt2), `tools/analyze/test_salt2_pm5_holdout_inputs_and_f2_score.py`, `work_products/2026-07/2026-07-23/2026-07-23_salt2_pm5_holdout_inputs_and_f2_score/**`, and USER-AUTHORIZED additive relabel (status/superseded_by frontmatter + off-scope banner only, no data change) to `work_products/2026-07/2026-07-23/2026-07-23_empirical_bias_salt1_4_refit_holdout_external_score/README.md` and `work_products/2026-07/2026-07-23/2026-07-23_combined_best_current_model_temp_mdot_correction/README.md`. READ-ONLY: val_salt2 1D config/operating point, val_salt2 CFD reference_k (`2026-06-23_ethan_frozen_state_1d_validation/cfd_sensor_reference.csv`), the frozen all-4 CFD affine, `../cfd-modeling-tools/tamu_first_order_model/Fluid/**` (read/run only), native CFD/OpenFOAM outputs, registry/admission state, scheduler state, thesis current/LaTeX files, and all other paths. No native-output mutation, registry/admission mutation, scheduler/solver launch, Fluid/external edit, thesis edit, physical closure/release/freeze/score, refit of the frozen global CFD affine, hidden multiplier, S11/S15/S6 trigger, deletion, staging, commit, or push. | Global-only per user. Score the frozen all-4 CFD affine (a=0.7669693, b=29.8909) on val_salt2 CFD reference_k as a genuine external test (resolve val_salt2's own 1D operating point first; do NOT reuse Salt2 nominal 1D). Relabel the two experiment-basis packages as off-scope/superseded for the CFD-only main body. STATUS: COMPLETE 2026-07-23. Global all-4 CFD affine (a=0.7669693, b=29.8909): Salt2+/-5Q holdout MAE 7.04 K (92.6% vs raw 95.1) and val_salt2 external MAE 7.62 K / RMSE 10.25 K (91.6% vs raw 91.0, n=15). val_salt2 1D on CFD-matching salt_jin default (mdot 0.02219, not refit's salt_current 0.0196); CFD target re-extracted with this session's pipeline. METHOD-CONSISTENCY FINDING: prior 2026-06-23 val extraction gave 29.3 K = vintage artifact; same-pipeline gives 7.62 K. Relabeled empirical_bias_salt1_4_refit (off-scope experiment-basis) and combined_best_current_model (temp off-scope/superseded, mdot CFD-basis retained). 5 tests pass. Partial-external caveat noted (val_salt2 = Salt2-nominal operating point). No physical closure/release/freeze/score/trigger or native/registry/scheduler/Fluid/thesis mutation. |
| TODO-COMBINED-BEST-CURRENT-MODEL-TEMP-MDOT-CORRECTION-2026-07-23 | Forward-pred / Implementer / Tester / Writer / Reviewer | claude / complete 2026-07-23 | `.agent/BOARD.md` (own row only), `.agent/status/2026-07-23_TODO-COMBINED-BEST-CURRENT-MODEL-TEMP-MDOT-CORRECTION-2026-07-23.md`, `.agent/journal/2026-07-23/combined-best-current-model-temp-mdot-correction.md`, `imports/2026-07-23_combined_best_current_model_temp_mdot_correction.json`, `operational_notes/07-26/23/2026-07-23_COMBINED_BEST_CURRENT_MODEL_TEMP_MDOT_CORRECTION.md`, `tools/analyze/build_combined_best_current_model_temp_mdot_correction.py`, `tools/analyze/test_combined_best_current_model_temp_mdot_correction.py`, `work_products/2026-07/2026-07-23/2026-07-23_combined_best_current_model_temp_mdot_correction/**`. READ-ONLY: `2026-07-23_empirical_bias_salt1_4_refit_holdout_external_score/**` (source F2 refit/holdout results, not modified), `2026-07-20_tswfc2_bounded_nominal_scorecard/case_outputs/**` (Salt1-4 nominal mdot ground truth), `2026-07-17_predict_salt2_pm5_holdout_extraction_repair/salt2_pm5_admission_table.csv`, `../cfd-modeling-tools/tamu_first_order_model/Fluid/**` (read/run only, includes `friction_closures.py` F3_hagenbach/F4/F5 forms cited as future work, no edits), the concurrent F2-freeze/PM5-holdout-inputs/S13-GCI packages (do not touch), canonical split policy, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, thesis current/LaTeX files, blocker register source, generated docs index files, and all other paths. No native-output mutation, registry/admission mutation, scheduler action, solver/postprocessing/sampler/harvest launch, Fluid/external edit (read/run the existing solver only), thesis current/LaTeX edit, physical source/property/Qwall release, physical candidate freeze, physical-closure claim, S11/S15/S6 trigger, blocker-register source change before closeout, generated-index refresh before closeout, deletion, staging, commit, or push. Mass-flow correction is fit AND scored on the SAME 4 Salt1-4 nominal points (no held-out mass-flow ground truth exists for salt2_lo5q/hi5q/val_salt2) and must be documented as in-sample-only, unlike the temperature correction which is genuinely holdout/external validated — do not conflate the two evidentiary tiers. | User explicitly asked for the combined best-current empirical-ROM model (temperature AND mass-flow corrections together) to share now, with future work named separately. Package combines the Salt1-4-refit F2 temperature correction (holdout/external validated, MAE 3.2-5.9 K) with a newly-fit mass-flow multiplicative correction (a=0.7504, in-sample MAE ~3.2% vs 32.6% raw, fit on the only 4 ground-truth mdot points that exist, Salt1-4 nominal) into one combined-model statement, explicitly flagging the mdot correction as NOT holdout-validated (no target data exists; local pm5-station Re values were considered and rejected as a back-calculation source because those stations show 37-79% local reverse-flow fraction and are explicitly non-single-stream per the project's own admission criteria). Also documents the root-cause finding (uncorrected 64/Re friction closure, F3_hagenbach/F4/F5 already implemented but unused) and a concrete future-work sequence. Empirical discrepancy/digital-twin-ROM diagnostic only; not a physical closure; not a legitimate protected-split freeze. STATUS: COMPLETE 2026-07-23. Verified every cited number against its source file (all matched exactly; none required correction): F2_global_affine Salt1-4-refit temperature correction (a=0.5746889644933884, b=138.28222646893295), holdout MAE 3.50/3.17 K (salt2_lo5q/hi5q), external MAE 5.87 K (val_salt2, n=15); mass-flow 1-DOF multiplicative correction a=0.7504257967710826 (recomputed least-squares from the 4 Salt1-4 nominal mdot pairs, raw mean relative error 32.57% -> corrected 3.17%, rejected 2-DOF affine alternative documented for transparency); friction root-cause f*Re=64.0 exactly for all 4 cases confirming the plain F1 (64/Re) closure is in use with F3_hagenbach/F4_leg_class/F5_ri_corrected implemented but unwired; F6 pure-multiplier temperature ablation confirmed NOT robust (good on one +/-5%Q direction, poor on the other, under either fit basis), so the F2 offset is load-bearing. Wrote the full work_products package (README with EVIDENTIARY TIER CALLOUT, summary.json, combined_model_spec.csv, mdot_correction_fit.csv, temp_correction_holdout_scores.csv, f6_pure_multiplier_ablation.csv, friction_root_cause.csv, claim_boundary_table.csv, evidentiary_tier_table.csv, future_work_sequence.csv, source_manifest.csv, no_mutation_guardrails.csv), the build script (no new fitting beyond the declared mdot multiplier, no solver runs), and the test script (9 checks). All 9 tests pass. Did not touch any other board row files. |
| TODO-EMPIRICAL-BIAS-SALT1-4-REFIT-HOLDOUT-EXTERNAL-SCORE-2026-07-23 | Forward-pred / Implementer / Tester / Writer / Reviewer | claude / complete 2026-07-23 | `.agent/BOARD.md` (own row only), `.agent/status/2026-07-23_TODO-EMPIRICAL-BIAS-SALT1-4-REFIT-HOLDOUT-EXTERNAL-SCORE-2026-07-23.md`, `.agent/journal/2026-07-23/empirical-bias-salt1-4-refit-holdout-external-score.md`, `imports/2026-07-23_empirical_bias_salt1_4_refit_holdout_external_score.json`, `operational_notes/07-26/23/2026-07-23_EMPIRICAL_BIAS_SALT1_4_REFIT_HOLDOUT_EXTERNAL_SCORE.md`, `tools/analyze/build_empirical_bias_salt1_4_refit_holdout_external_score.py`, `tools/analyze/test_empirical_bias_salt1_4_refit_holdout_external_score.py`, `work_products/2026-07/2026-07-23/2026-07-23_empirical_bias_salt1_4_refit_holdout_external_score/**`. READ-ONLY: `2026-07-21_fluid_reduced_dof_bias_transfer_screen/{fit_and_transfer_sensor_rows.csv,frozen_coefficients.csv}` (source data and original Salt1/2-fit comparison baseline, not modified), `2026-07-17_predict_salt2_pm5_holdout_extraction_repair/salt2_pm5_admission_table.csv`, `2026-07-17_val_salt2_external_score_and_unlock_progress/val_salt2_external_score_targets.csv`, the concurrently active `2026-07-23_salt2_pm5_holdout_inputs_and_f2_score/**` and `2026-07-23_f2_empirical_holdout_freeze_and_score/**` packages (do not touch; different task), `../cfd-modeling-tools/tamu_first_order_model/Fluid/**` (read/run for prediction generation only, no edits), canonical split policy, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, thesis current/LaTeX files, blocker register source, generated docs index files, and all other paths. No native-output mutation, registry/admission mutation, scheduler action, solver/postprocessing/sampler/harvest launch, Fluid/external edit, thesis current/LaTeX edit, physical source/property/Qwall release, physical candidate freeze, physical-closure claim, coefficient admission as a physical HTC, S11/S15/S6 trigger, hidden multiplier beyond the declared affine/offset forms, blocker-register source change before closeout, generated-index refresh before closeout, deletion, staging, commit, or push. This is an explicit user-directed refit (new fitting exercise, override of default no-refit-after-freeze discipline) and scores against the same Salt2 +/-5Q / val_salt2 rows already touched once this session by a prior override pass on the original Salt1/2-fit F1-F5 family — must be documented as a second holdout/external exposure within this session, not a legitimate single-use freeze score. | User explicitly requested a refit of the F1-F5-style empirical bias-correction family using ALL FOUR Salt1-4 nominal train sensor rows (the canonical train set) instead of the existing family's Salt1/Salt2-only fit, then scoring against the same Salt2 +/-5Q holdout and val_salt2 external targets already extracted, with results compared directly against the original fit and thoroughly documented (refit coefficients, per-family MAE/RMSE old-vs-new, holdout-exposure-count caveat, exact allowed/forbidden thesis claims). Empirical discrepancy/digital-twin-ROM diagnostic only; explicitly not a physical closure and not a legitimate protected-split freeze score (both holdout and external rows have now been used more than once for model comparison within this override). STATUS: COMPLETE 2026-07-23. Refit F0-F5 on all 64 usable Salt1-4 nominal rows (up from 32 for Salt1/2 only); raw 1D predictions for salt2_lo5q/hi5q/val_salt2 reproduced via solve_case() and cross-checked exactly against the cited prior-pass old-fit F0/F2/F5 MAE (lo5q F0=89.44/F2=3.47, hi5q F0=100.34/F5=2.89, val_salt2 F0=90.65/F2=6.45). New Salt1-4-refit F2_global_affine (a=0.5746889644933884, b=138.28222646893295) wins ALL THREE dimensions: holdout mean MAE=3.34 K (lo5q 3.50, hi5q 3.17), external val_salt2 MAE=5.87 K (n=15), robustness range=2.70 K across all 3 splits -- unlike the old Salt1/2 fit where F3/F5/F2 each won a different split. F1/F3/F4 (offset-only) got worse at holdout under the larger refit; F2 (pure affine) was most stable. Recommend F2_global_affine (Salt1-4 refit) as the single family to carry forward, reported strictly as empirical-ROM, never a physical closure, never a legitimate single-use protected score (documented second exposure of these rows this session). 7 tests pass; 18 guardrails False; did not touch the separate F2-freeze effort's files. |
| TODO-SALT2-PM5-HOLDOUT-INPUTS-AND-F2-SCORE-2026-07-23 | Forward-pred / cfd-pp / Implementer / Tester / Writer / Reviewer | claude / in progress 2026-07-23 | `.agent/BOARD.md` (own row only), `.agent/status/2026-07-23_TODO-SALT2-PM5-HOLDOUT-INPUTS-AND-F2-SCORE-2026-07-23.md`, `.agent/journal/2026-07-23/salt2-pm5-holdout-inputs-and-f2-score.md`, `imports/2026-07-23_salt2_pm5_holdout_inputs_and_f2_score.json`, `operational_notes/07-26/23/2026-07-23_SALT2_PM5_HOLDOUT_INPUTS_AND_F2_SCORE.md`, `tools/analyze/build_salt2_pm5_holdout_inputs_and_f2_score.py`, `tools/analyze/test_salt2_pm5_holdout_inputs_and_f2_score.py`, `work_products/2026-07/2026-07-23/2026-07-23_salt2_pm5_holdout_inputs_and_f2_score/**`, and a fresh dated staging copy under `staging/2026-07-23_salt2_pm5_reconstruct/**` for reconstruction only. READ-ONLY (no mutation): native `salt2_jin_lo5q_corrected`/`salt2_jin_hi5q_corrected` case trees (processors64/constant/system) copied out before reconstructPar, frozen F2 manifest, TSWFC2 nominal scorecard scenario, 1D model `../cfd-modeling-tools/tamu_first_order_model/Fluid/**`, canonical split policy, registry/admission state, other native runs, Fluid source edits, thesis current/LaTeX files, blocker register source, generated docs index files until closeout, and all other paths. No native case_stage mutation (reconstruct only in the staged copy), registry/admission mutation, scheduler/solver launch, Fluid/external edit, thesis edit, physical source/property/Qwall release, physical candidate freeze, refit or model selection after F2 freeze, hidden multiplier beyond frozen affine, physical-closure claim for F2, S11/S15/S6 trigger, blocker-register source change before closeout, generated-index refresh before closeout, deletion, staging over other campaigns, commit, or push. | Produce the legal protected-holdout inputs and F2 score for Salt2 +/-5Q: (1) generate blind 1D predictions at TP/TW sensors for lo5q (heater 252.415 W) and hi5q (278.985 W); (2) reconstruct the terminal stationary window (lo5q 10275 s, hi5q 9780 s) in a STAGED copy and sample CFD T at TP/TW mesh-centerline locations to build the measured targets; (3) apply frozen F2 once and report holdout RMSE/MAE as empirical-ROM performance (no physical-closure claim). STATUS: COMPLETE 2026-07-23. Scope evolved to CFD-basis per user steer (experiment out of scope for main body). Registered 2 pm5 profiles in case_analysis_profiles.py (user-authorized; mesh byte-identical to nominal Salt2). Fixed numpy-2.0 np.trapz in the shared landmark extractor. Extracted CFD reference_k (OF13 TP/TW sampling) for Salt2 lo5q/hi5q + nominal Salt1-4 (non-mutating staging mirrors). Fit all-4 nominal CFD affine a=0.7669693 b=29.8909 (60 pairs); Salt2+/-5Q CFD holdout combined MAE 7.04 K / RMSE 9.27 K (15 TP/TW sensors/case) vs raw 1D 95.1 K = 92.6% reduction; orig Salt1/2 exp-fit F2 6.01 K locally. val_salt2 external DEFERRED (needs own 1D op point). 5 tests pass. RECONCILIATION: supersedes the experiment-basis 2026-07-23_empirical_bias_salt1_4_refit_holdout_external_score for the CFD-only main body. No physical closure/release/freeze/score, native/registry/scheduler/Fluid/thesis mutation, or trigger. |
| TODO-F2-EMPIRICAL-HOLDOUT-FREEZE-AND-SCORE-HARNESS-2026-07-23 | Forward-pred / Implementer / Tester / Writer / Reviewer | claude / in progress 2026-07-23 | `.agent/BOARD.md` (own row only), `.agent/status/2026-07-23_TODO-F2-EMPIRICAL-HOLDOUT-FREEZE-AND-SCORE-HARNESS-2026-07-23.md`, `.agent/journal/2026-07-23/f2-empirical-holdout-freeze-and-score-harness.md`, `imports/2026-07-23_f2_empirical_holdout_freeze_and_score_harness.json`, `operational_notes/07-26/23/2026-07-23_F2_EMPIRICAL_HOLDOUT_FREEZE_AND_SCORE_HARNESS.md`, `tools/analyze/build_f2_empirical_holdout_freeze_and_score.py`, `tools/analyze/test_f2_empirical_holdout_freeze_and_score.py`, `work_products/2026-07/2026-07-23/2026-07-23_f2_empirical_holdout_freeze_and_score/**`. READ-ONLY: `2026-07-21_fluid_reduced_dof_bias_transfer_screen/frozen_coefficients.csv`, `2026-07-20_tswfc2_bounded_nominal_scorecard/case_outputs/**`, canonical final predictive split policy, holdout split/freeze policy, mf11 empirical attribution gate, Salt2 pm5 / val_salt2 target packages, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, Fluid/external repos, thesis current/LaTeX files, blocker register source, generated docs index files until closeout, and all other paths. No native-output mutation, registry/admission mutation, scheduler action, solver/postprocessing/sampler/harvest launch, Fluid/external edit, thesis current/LaTeX edit, physical source/property/Qwall release, physical candidate freeze, protected/final scoring before required inputs land, refit or model selection, hidden multiplier beyond the frozen affine, physical-closure claim for F2/F5, S11/S15/S6 trigger firing, blocker-register source change before closeout, generated-index refresh before closeout, deletion, staging, commit, or push. | Freeze F2's two coefficients (a=0.37298, b=246.552) into an immutable pre-registration manifest and build + dry-run-validate the score-once harness on nominal data; emit a holdout-score readiness ledger naming the exact remaining inputs (1D +/-5Q predictions + TP/TW CFD target extraction for Salt2 +/-5Q; external artifact for val_salt2). Empirical discrepancy diagnostic only; scores NO protected row yet. STATUS: COMPLETE 2026-07-23. F2 frozen (a=0.3729829182408737, b=246.55192842685844, 2 DOF, Salt1/2 fit, content_sha256 3f4cc72f...); score-once harness built + dry-run-validated on Salt2 nominal (corrected RMSE 16.46 K vs ~90 K raw; affine identity check passed); 6 tests pass; 15 guardrails False. 0 protected rows scored. Sole blocker to the real holdout number: TP/TW CFD sensor target extraction for Salt2 +/-5Q (existing runs, no new solve) + 1D +/-5Q predictions; val_salt2 additionally needs a missing external sensor artifact. No refit/selection, physical-closure claim, source/property/Qwall release, physical freeze, native/registry/scheduler/Fluid/thesis mutation, or trigger. |
| TODO-S13-STATIONARY-WINDOW-QWALL-MESH-GCI-2026-07-23 | Hydraulics / Thermal-modeling / Mesh-GCI / Implementer / Tester / Writer / Reviewer | claude / in progress 2026-07-23 | `.agent/BOARD.md` (own row only), `.agent/status/2026-07-23_TODO-S13-STATIONARY-WINDOW-QWALL-MESH-GCI-2026-07-23.md`, `.agent/journal/2026-07-23/s13-stationary-window-qwall-mesh-gci.md`, `imports/2026-07-23_s13_stationary_window_qwall_mesh_gci.json`, `operational_notes/07-26/23/2026-07-23_S13_STATIONARY_WINDOW_QWALL_MESH_GCI.md`, `tools/analyze/build_s13_stationary_window_qwall_mesh_gci.py`, `tools/analyze/test_s13_stationary_window_qwall_mesh_gci.py`, `work_products/2026-07/2026-07-23/2026-07-23_s13_stationary_window_qwall_mesh_gci/**`. READ-ONLY: S13 direct-coarse-extraction / medium-fine-exact-label-split-rerun / medium-fine-mesh-gci-disposition / true-same-physical-window packages, salt mesh refinement discovery inventory, `tools/analyze/compute_gci.py`, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, Fluid/external repos, thesis current/LaTeX files, blocker register source, generated docs index files until closeout, and all other paths. No native-output mutation, registry/admission mutation, scheduler action, solver/postprocessing/sampler/harvest launch, Fluid/external edit, thesis current/LaTeX edit, source/property/Qwall/numeric q-loss release, coefficient admission, candidate freeze, protected/final scoring, fitting/tuning/model selection, hidden multiplier, borrowed/fabricated GCI or monotonicity, mesh-GCI admission beyond what the asymptotic-range check supports, S11/S15/S6 trigger firing, blocker-register source change before closeout, generated-index refresh before closeout, deletion, staging, commit, or push. | Compute a formal stationary-terminal-window mesh GCI for the S13 seeded exchange-CV QOIs (Q_wall + exchange/recirc proxies) across the existing coarse/medium/fine Jin meshes, justified by demonstrated terminal-window stationarity that removes the same-absolute-time equivalence blocker for steady QOIs. Admit mesh-UQ only where the ASME asymptotic-range check passes. STATUS: COMPLETE 2026-07-23. Q_wall mesh-UQ ADMITTED for Salt2/3/4 (GCI_fine 0.0005/0.51/0.36%, observed order p 10.2/2.06/2.02, asymptotic ratio ~0.99) via demonstrated terminal-window stationarity (half-range <=0.003%) — first admissible GCI; loop wall-heat mesh-independent to <=0.51%. Recirculation-exchange proxies fail-closed (GCI 79-768%, mesh-unconverged). same_qoi mesh_gci_gate passes for Q_wall only. No source/property/Qwall release, freeze, score, trigger, fitting, hidden multiplier, borrowed GCI, or native/registry/scheduler/Fluid/thesis mutation; 16 guardrails False; 6 tests pass. |
| TODO-THESIS-TWO-TRACK-ROM-MODEL-WRITEUP-2026-07-23 | Writer / Reviewer / Forward-pred | codex / complete 2026-07-23 | `.agent/BOARD.md` (own row only), `.agent/status/2026-07-23_TODO-THESIS-TWO-TRACK-ROM-MODEL-WRITEUP-2026-07-23.md`, `.agent/journal/2026-07-23/thesis-two-track-rom-model-writeup.md`, `imports/2026-07-23_thesis_two_track_rom_model_writeup.json`, `work_products/2026-07/2026-07-23/2026-07-23_thesis_two_track_rom_model_writeup/**`. READ-ONLY: empirical bias publication report, reduced-DOF transfer screen, thesis master model-form scoreboard, D2/D3/D4/MF11 gates, predictive blocker burndown, P1D/PASSIVE-H2 gate packages, thesis current files, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, Fluid/external repos, blocker register source, generated docs index files, and all other paths. No native-output mutation, registry/admission mutation, scheduler action, solver/postprocessing/sampler/harvest/UQ launch, Fluid/external edit, thesis current/LaTeX edit, new fitting/tuning/model selection, validation/holdout/external-test scoring, source/property release, coefficient admission, candidate freeze, final-score claim, S11/S12/S13/S15/S6 trigger, blocker-register source change, generated-index refresh before closeout, deletion, staging, commit, push, runtime-leakage relaxation, or residual absorption into internal Nu. | Completed one-hour thesis-facing two-track ROM model writeup. Published `work_products/2026-07/2026-07-23/2026-07-23_thesis_two_track_rom_model_writeup/` with liftable prose, model comparison table, claim-boundary table, and source manifest. Track A: `F2_global_affine` recommended as main empirical bias-corrected ROM, `F5_thermal_family_offset_shared_multiplier` as best numerical upper bound; Track B: steady `fluid+walls` / `P1D-BULK-CV-H2` / `PASSIVE-H2-CAND001` as strict physical path with M6 still blocked. No new fitting/scoring/admission/freeze/source release/compute/native/registry/scheduler/Fluid/thesis-current mutation. STATUS: COMPLETE 2026-07-23. |
| TODO-PASSIVE-H2-STRICT-SOURCE-ENVELOPE-AND-RELEASE-UQ-CONVERSION-2026-07-22 | Forward-pred / Thermal-modeling / Implementer / Tester / Writer / Reviewer | claude / in progress 2026-07-22 | `.agent/BOARD.md` (own row only), `.agent/status/2026-07-22_TODO-PASSIVE-H2-STRICT-SOURCE-ENVELOPE-AND-RELEASE-UQ-CONVERSION-2026-07-22.md`, `.agent/journal/2026-07-22/passive-h2-strict-source-envelope-and-release-uq-conversion.md`, `imports/2026-07-22_passive_h2_strict_source_envelope_and_release_uq_conversion.json`, `operational_notes/07-26/22/2026-07-22_PASSIVE_H2_STRICT_SOURCE_ENVELOPE_AND_RELEASE_UQ_CONVERSION.md`, `tools/analyze/build_passive_h2_strict_source_envelope_and_release_uq_conversion.py`, `tools/analyze/test_passive_h2_strict_source_envelope_and_release_uq_conversion.py`, `work_products/2026-07/2026-07-22/2026-07-22_passive_h2_strict_source_envelope_and_release_uq_conversion/**`. READ-ONLY: all PASSIVE-H2 R4 predeclared / post-junction / mesh-area-provenance-repair / subspan-mapping / salt1-branch-recovery / diagnostic-freeze packages, the same-QOI UQ phase-B/phase-C matrices, `tools/analyze/compute_gci.py`, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, Fluid/external repos, thesis current/LaTeX files, blocker register source, generated docs index files until closeout, and all other paths. No native-output mutation, registry/admission mutation, scheduler action, solver/postprocessing/sampler/harvest/UQ launch, Fluid/external edit, thesis current/LaTeX edit, source/property/Qwall/numeric q-loss release, coefficient admission, candidate freeze, protected/final scoring, validation/holdout/external scoring, fitting/tuning/model selection, hidden multiplier, wallHeatFlux runtime use, residual absorption into internal Nu, S11/S15/S6 trigger firing, blocker-register source change before closeout, generated-index refresh before closeout, deletion, staging, commit, or push. | Convert the diagnostic PASSIVE-H2 R4 (junction-excluded, four-family) evidence into (a) a strict, wallHeatFlux/postProcessing-free source-envelope table — explicit source envelope, salt_jin property label, mesh-backed area/subspan basis, per-family provenance, split role, and per-row strict pass/fail; and (b) a release-grade same-QOI UQ readiness table for the R4 QOI labels, or a rigorous fail-closed determination naming the exact missing evidence. Keeps release/freeze/score at False. STATUS: COMPLETE 2026-07-22. Strict source-envelope converted to 8/16 PASS (Salt1+Salt2 train x 4 retained families) via wallHeatFlux-free mesh/setup-backed provenance — first non-zero strict count; Salt3/Salt4 fail-closed on c3 (protected). Same-QOI release UQ fail-closed with source_gate now passing for train, isolating the sole freeze blocker to a same-QOI 3-mesh GCI triplet (medium/fine CFD at Salt2 physical window; S13 found 0 exact targets). source_property_release/freeze/score all False; 15 guardrails False; 8 tests pass. No release/freeze/score/trigger, native/registry/scheduler/Fluid/thesis mutation, fitting, hidden multiplier, or runtime wallHeatFlux use. |
| TODO-THESIS-STUDY-S15-CANDIDATE-FREEZE-SOURCE-PROPERTY-SCORE-RELEASE-2026-07-21 | Forward-pred / Tester / Writer / Reviewer | open / trigger-gated after S12, S13, or S14 releases exactly one candidate | `.agent/BOARD.md` (own row only), `.agent/status/<date>_TODO-THESIS-STUDY-S15-CANDIDATE-FREEZE-SOURCE-PROPERTY-SCORE-RELEASE-2026-07-21.md`, `.agent/journal/<date>/thesis-study-s15-candidate-freeze-source-property-score-release.md`, `imports/<date>_thesis_study_s15_candidate_freeze_source_property_score_release.json`, `work_products/<date>_thesis_study_s15_candidate_freeze_source_property_score_release/**`; optional task-owned runner/checker only after exact filenames are claimed. READ-ONLY: S11 candidate-specific release row, S6 blocked scorecard shell, final split policy, source/property ledgers, candidate-producing S12/S13/S14 package, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, Fluid source tree, external repos, blocker register, generated docs index files, and all other paths. No work before exactly one candidate is named; no native-output mutation, registry/admission mutation, scheduler action, solver/postprocessing launch, Fluid edit, external edit, holdout/external tuning, broad release of protected rows, final score claim before freeze, blocker-register change, generated-index refresh, or runtime-leakage relaxation. | Scientific requirement: convert a single admitted candidate into a frozen score-release decision, or document why final scoring remains blocked. Required analysis: candidate-specific source validity, property sensitivity, split-role permissions, runtime-input audit, protected-row audit, uncertainty/status table, frozen manifest, and scorecard release decision. Required figures/tables: source/property release ledger, frozen-candidate manifest, split/protected-row audit table, uncertainty table, and final scorecard or blocked-scorecard update. Acceptance: either S6 is opened with one predeclared frozen runtime-legal candidate, or the thesis records a rigorous no-freeze result with `0` final score values. |
| TODO-THESIS-STUDY-S11-CANDIDATE-SOURCE-PROPERTY-REFRESH-2026-07-21 | Forward-pred / Reviewer / Writer / Tester | open / trigger-gated after S8, S9, or S10 produces an admission-worthy physical candidate | `.agent/BOARD.md` (own row only), `.agent/status/<date>_TODO-THESIS-STUDY-S11-CANDIDATE-SOURCE-PROPERTY-REFRESH-2026-07-21.md`, `.agent/journal/<date>/thesis-study-s11-candidate-source-property-refresh.md`, `imports/<date>_thesis_study_s11_candidate_source_property_refresh.json`, `work_products/<date>_thesis_study_s11_candidate_source_property_refresh/**`; optional repo-local builder/checker under `tools/analyze/` only after exact filenames are claimed. READ-ONLY: completed S5 release package, candidate-producing S8/S9/S10 package, final predictive split policy, source/property ledgers, final scorecard shell, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, Fluid source tree, external repos, generated docs index files, and all other paths. No native-output mutation, registry/admission mutation, scheduler action, solver/postprocessing launch, Fluid edit, external edit, holdout/external tuning, broad release of protected rows, final score claim, blocker-register change, or generated-index refresh. | S11 study: run a narrow source/property/split refresh for exactly one admission-worthy candidate before any freeze. Acceptance: candidate-specific source validity and property sensitivity pass; holdout, external, future, and support rows remain score-only where required; result either releases a predeclared runtime-legal freeze candidate for S6 or documents why no candidate exists. |
| TODO-THESIS-CSEM-POST-FREEZE-NARRATIVE-REFRESH | Writer / Reviewer / Forward-pred | open / trigger-gated | `.agent/BOARD.md` (own row only), `.agent/status/<date>_TODO-THESIS-CSEM-POST-FREEZE-NARRATIVE-REFRESH.md`, `.agent/journal/<date>/thesis-csem-post-freeze-narrative-refresh.md`, `imports/<date>_thesis_csem_post_freeze_narrative_refresh.json`, `reports/thesis_dossier/Chapters_and_sections/current/22_csem_post_freeze_predictive_results_addendum.md`, optional additive updates to chapter files only after no active chapter row owns them. READ-ONLY until trigger: CSEM dispatch note, `14_csem_narrative_integration_plan.md`, final predictive scorecard shell, future frozen scorecard package, claim ledger, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, Fluid source tree, and all other paths. No work before a frozen prediction artifact exists; no fitting/tuning/model selection from writing row. | After a frozen predictive scorecard lands, write the thesis-safe final predictive-results addendum: train/support/holdout/external residuals, runtime audit, source/property labels, uncertainty, and what changed relative to the blocked shell. Acceptance: no held-out or external row is used for fitting/model selection, and final claims cite the frozen scorecard source paths. |
| TODO-THESIS-CSEM-PRESSURE-ADMISSION-REFRESH | Writer / Reviewer / Hydraulics | open / trigger-gated | `.agent/BOARD.md` (own row only), `.agent/status/<date>_TODO-THESIS-CSEM-PRESSURE-ADMISSION-REFRESH.md`, `.agent/journal/<date>/thesis-csem-pressure-admission-refresh.md`, `imports/<date>_thesis_csem_pressure_admission_refresh.json`, `reports/thesis_dossier/Chapters_and_sections/current/23_csem_pressure_admission_refresh.md`, optional additive updates to pressure chapter files only after no active chapter row owns them. READ-ONLY until trigger: CSEM dispatch note, `13_two_tap_recirc_section_effective_pressure_model.md`, LitRev pressure findings, `.agent/BLOCKERS.md`, future two-tap/F6 admission packages, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, Fluid source tree, and all other paths. No component-K, F6, or section-effective admission from writing alone. | After same-QOI UQ, component isolation, low-recirculation/nonrecirculating anchor, or accepted section-effective scoring evidence lands, refresh the pressure narrative. Acceptance: states whether ordinary component K remains blocked or a named pressure model is admitted, with pressure basis, velocity basis, source envelope, split role, and uncertainty status. |
| TODO-THESIS-CSEM-WALL-TS-CLOSURE-REFRESH | Writer / Reviewer / Thermal-modeling | open / trigger-gated | `.agent/BOARD.md` (own row only), `.agent/status/<date>_TODO-THESIS-CSEM-WALL-TS-CLOSURE-REFRESH.md`, `.agent/journal/<date>/thesis-csem-wall-ts-closure-refresh.md`, `imports/<date>_thesis_csem_wall_ts_closure_refresh.json`, `reports/thesis_dossier/Chapters_and_sections/current/24_csem_wall_test_section_closure_refresh.md`, optional additive updates to thermal chapter files only after no active chapter row owns them. READ-ONLY until trigger: CSEM dispatch note, `07_wall_test_section_coupled_score_and_physics_plan.md`, `10_uncertainty_chapter_package.md`, `.agent/BLOCKERS.md`, future wall/test-section candidate packages, native CFD/OpenFOAM outputs, registry/admission state, scheduler state, Fluid source tree, and all other paths. No closure admission from writing alone and no runtime leakage. | After a runtime-legal wall/test-section/passive-boundary candidate is admitted or definitively falsified, refresh the thermal narrative. Acceptance: reports mdot, TP/TW, all-probe, heat-ledger, uncertainty, and admission status while preserving the ban on realized CFD wallHeatFlux, CFD mdot, imposed cooler duty, realized test-section heat, and validation temperatures as runtime inputs. |
## Planned / Unclaimed

These rows are ready for future agents to claim. A worker must claim exactly one row, update the owner/status fields, create its own status file, and stay inside the listed edit paths. Detailed briefs live in `operational_notes/07-26/07/2026-07-07_cfd_postprocessing_closure_rigor_deep_dive.md` under `Draft Board-Ready Agent Briefs`.

Worker closeout requirement: every task must end with observed facts, inferred interpretation, blockers, exact files used, and a recommended next action in its status or journal handoff.

External-writer evidence-packet requirement: any row intended to support thesis/LaTeX prose must produce or consume a compact evidence packet with exact numbers, source paths, governing equations, definitions, assumptions, caveats, allowed/forbidden claims, split/admission state, runtime-leakage audit, and figure/table targets. Use `work_products/2026-07/2026-07-22/2026-07-22_thesis_external_writer_evidence_packet_contract/evidence_packet_schema.csv` as the required schema. Codex-owned rows in this repo must not be scoped as actual LaTeX prose writing or full thesis chapter composition. They may prepare evidence packets, copy/no-copy manifests, caption/source ledgers, assumptions/caveats, and writer instructions for an outside writer. Do not move huge raw CFD/native/generated trees into the LaTeX repo; only selected small packets, CSV excerpts, tables, captions, and selected figures may be proposed under a separate papers-board artifact-transfer row.

| Task ID | Priority | Role | Owner | Allowed edit paths | Required read-only context | Objective / acceptance summary |
| --- | --- | --- | --- | --- | --- | --- |
## Blocked

| Task ID | Role | Owner | Scope | Blocker |
| --- | --- | --- | --- | --- |
None currently.
## Archive

Historical archived board rows live in `.agent/BOARD_ARCHIVE.md`.
Do not append completed archive blocks to the live board; run `python3.11 tools/agent/board_archive.py --apply` after board hygiene.
## Board Rules

- Coordinators assign tasks and non-overlapping file ownership before edits.
- Implementers, Testers, Writers, Reviewers, and Cleaners must claim exactly
  one task at a time unless the coordinator records otherwise here.
- Coordinator note: `codex` is holding `TODO-THESIS-STUDY-THERMAL-TW-AFTER-TP-RESIDUAL-OWNERSHIP-2026-07-22` and `TODO-THESIS-STUDY-PRESSURE-LOW-RECIRC-ANCHOR-DESIGN-AND-HARVEST-2026-07-22` together because the user requested more board studies, both are P0 thesis studies, and both can be advanced from existing evidence without scheduler or native-output mutation.
- Coordinator note: `codex` is holding `TODO-THESIS-STUDY-UPCOMER-ONSET-ANCHOR-DESIGN-AND-RECIRC-FRACTION-UQ-2026-07-22` and `TODO-THESIS-STUDY-FIGURE-TABLE-FINAL-INCORPORATION-LEDGER-2026-07-22` together because the user explicitly requested a couple of board studies, the scopes are additive and non-overlapping, and both can close from existing evidence without scheduler or native-output mutation.
- Coordinator note: `codex` held the paired `AGENT-016` and `AGENT-017`
  tasks together because the user explicitly requested both the repair and a
  detailed same-day journal write-up for the same scoped change.
- Coordinator note: `codex` is holding the paired `AGENT-018` and
  `AGENT-019` tasks together because the user requested the remaining full
  repair and the matching same-day documentation should stay aligned with the
  implementation.
- Coordinator note: `codex` is holding the paired `AGENT-021` and
  `AGENT-022` tasks together because the user requested both new report-prep
  analysis outputs and updated same-day methodology documentation.
- Coordinator note: `codex` is holding `AGENT-116` alongside `AGENT-102`
  because the user requested the same-turn presentation refresh on the local
  1D bakeoff package plus the stale external-Fluid bookkeeping boundary.
- Coordinator note: `codex` is holding `AGENT-119` in the same session because
  the user explicitly requested a write-up of assumptions, results, work
  underway, and next steps grounded in the already-built June 23 bakeoff
  package.
- Coordinator note: `codex` held `AGENT-129` in the same session because
  the user explicitly requested a backup workflow, documentation, and transfer
  estimate for the live workspace state.
- Coordinator note: `codex` is holding `AGENT-131` in the same session because
  the user explicitly requested a new pressure-drop table plus reusable
  documented math built on the existing case-analysis products.
- Coordinator note: `codex` is holding `AGENT-132` in the same session because
  the user explicitly requested a durable checkpoint journal to resume from
  tomorrow.
- Coordinator note: `codex` is holding `AGENT-121`, `AGENT-122`, and
  `AGENT-123` in the same session because the user explicitly requested a
  sequential June 23 latest-window freeze, a retargeted local 1D comparison
  stack, and a discrepancy explainer package built on that same refreshed
  frozen contract.
- Coordinator note: `codex` is holding `AGENT-138` in the same session because
  the user explicitly requested that the CFD-derived friction and HTC closure
  modeling effort continue, and the follow-on is scoped to new reusable bundle
  code plus `work_products/` outputs so it does not overlap the still-active
  June 23 bakeoff and discrepancy builders.
- Coordinator note: `codex` is holding `AGENT-124` in the same session because
  the user explicitly requested slide-ready 1D-modeling material while the
  latest-window refresh tasks are still in flight, and the presentation package
  needs to state that current evidence boundary precisely.
- Coordinator note: `codex` is holding `AGENT-125` alongside `AGENT-124`
  because the user immediately requested the exact `p_rgh`/`q_dyn` mean-method
  documentation and a sharper presentation-local 1D performance breakdown in
  the same turn.
- Coordinator note: `codex` is holding `AGENT-127` in the same session because
  the user explicitly requested git-only cleanup and push of the already-owned
  June workspace state, and the curation is restricted to excluding generated
  figure/image artifacts from the commit while preserving local files.
- Coordinator note: `codex` is holding the paired `AGENT-023` and
  `AGENT-024` tasks together because the user requested both the new
  streamwise thermal extension and same-day documentation of its method and
  interpretation boundaries.
- Coordinator note: `codex` is holding the paired `AGENT-025` and
  `AGENT-026` tasks together because the user requested both the matched
  cross-sectional bulk-temperature implementation and detailed report-facing
  documentation of the upgraded thermal method in the same pass.
- Coordinator note: `codex` is holding `AGENT-027` in the same session because
  the user explicitly requested an immediate reviewer pass on the rebuilt
  package before moving on to further report work.
- Coordinator note: `codex` is holding the paired `AGENT-029` and
  `AGENT-032` tasks together because the user explicitly requested the report
  path and the first reusable-script hardening pass in the same implementation
  turn.
- Coordinator note: `codex` is holding `AGENT-033` in the same session because
  it is the direct follow-on hardening fix from the completed `AGENT-032`
  slice and it owns a disjoint write scope.
- Coordinator note: `codex` is holding `AGENT-035` in the same session because
  it is the next non-overlapping reusable-script hardening slice after
  `AGENT-033` and it owns the shared profile-definition scope needed before
  broader Salt-family rollout.
- Coordinator note: `codex` is holding `AGENT-102` and `AGENT-114` together in
  this session because the user explicitly requested the refresh-first external
  replay lane and the local frozen-state 1D closure bakeoff / presentation
  report to advance in parallel, with the local package serving as the current
  reportable surface while the external `Fluid` refresh remains stale.
- Coordinator note: `codex` is holding `AGENT-036` in the same session because
  it is the next direct provenance-safety hardening slice after `AGENT-035`
  and it returns to the already-owned package-builder file without overlapping
  any other active owner.
- Coordinator note: `codex` is holding `AGENT-073` in the same session because
  the user explicitly requested an immediate narrow ParaView rendering-default
  refresh, the render slice files are not claimed by any other active task, and
  the work needs a same-pass provenance record plus bounded figure regeneration.
- Coordinator note: `codex` is holding `AGENT-075` in the same session because
  it is the direct follow-on renderer-capability expansion requested by the
  user, it stays inside the same unclaimed `tools/extract/render_last_timestep_field_slices.py`
  scope plus a bounded tmp smoke area, and it should remain adjacent to the
  just-completed cell-association refresh.
- Coordinator note: `codex` is holding `AGENT-079` in the same session because
  the user explicitly requested a new dissertation-grade pressure / HTC /
  boundary-layer analysis layer, and the implementation is deliberately scoped
  to a fresh additive builder plus a new dated report package so it does not
  reopen the actively owned June 15/17 extraction files.
- Coordinator note: `codex` is holding `AGENT-080` in the same session because
  the user explicitly requested the next Salt-only closure-first analysis
  phase, and the implementation is deliberately scoped to a fresh additive
  builder plus a new dated report package so it does not reopen the actively
  owned June 15/17 extraction or package-builder files.
- Coordinator note: `codex` is holding `AGENT-099`, `AGENT-100`, and
  `AGENT-103` in the same session because the user explicitly requested the
  roadmap plus immediate implementation, the three scopes are additive and
  non-overlapping, and the report lane depends directly on the new feature-path
  hardening results produced in the same turn.
- Coordinator note: `codex` is holding `AGENT-081` in the same session because
  the user explicitly requested the planned Salt analysis roadmap to be
  implemented with durable checkpoints and documentation along the way, and the
  implementation is deliberately scoped to a fresh additive builder plus a new
  dated checkpoint-suite package so it does not reopen the actively owned June
  15/17 extraction or package-builder files.
- Coordinator note: `codex` is holding `AGENT-084` in the same session because
  the user explicitly requested the next defensible model-dependency layer with
  audit-grade documentation, and the implementation is deliberately scoped to a
  fresh additive builder, tests, and a new dated report package so it does not
  reopen the actively owned June 15/17 extraction or earlier additive package
  builders.
- Coordinator note: `codex` is holding `AGENT-087` in the same session because
  the user explicitly requested the next full closure-to-modeling campaign, and
  the implementation is deliberately scoped to fresh additive analyzers, tests,
  and new dated report roots so it can harden Salt conclusions and parallel
  Water readiness without reopening the AGENT-072 shared extractor files.
- Coordinator note: `codex` is holding `AGENT-089` in the same session because
  the user explicitly requested an immediate blocker-first write-up, 1D-model
  roadmap, and next-CFD submission pass, and the implementation is
  deliberately scoped to a fresh dated report root plus a new campaign root so
  it does not reopen the AGENT-072-owned June 18 wave or shared extractor
  files.
- Coordinator note: `codex` is holding the paired `AGENT-039` and
  `AGENT-040` tasks together because the user explicitly requested
  checkpoint-by-checkpoint board and journal updates while the bounded
  reviewer-gate fix is implemented.
- Coordinator note: `codex` is holding `AGENT-041` in the same session because
  the autonomous queue requires an immediate reviewer rerun after the bounded
  heat-decoupling fix before any broader rollout work resumes.
- Coordinator note: `codex` is holding the paired `AGENT-042` and
  `AGENT-043` tasks together because the rerun reviewer pass reduced the
  remaining rollout boundary to one documentation/provenance issue spanning
  both machine-readable outputs and checkpoint prose.
- Coordinator note: `codex` is holding `AGENT-045` in the same session because
  the autonomous queue explicitly advances from a cleared Salt 2 hardening gate
  into the first Salt-family profile rollout candidate.
- Coordinator note: `codex` is holding `AGENT-046` in the same session because
  the autonomous queue requires an immediate reviewer gate on the first
  Salt-family rollout artifact before any broader family expansion.
- Coordinator note: `codex` is holding `AGENT-047` in the same session because
  the first Salt-family reviewer gate reduced the remaining rollout boundary to
  one bounded rebuild task on the full retained late window.
- Coordinator note: `codex` is holding `AGENT-049` in the same session because
  the Salt 1 Jin rerun reviewer gate is clear enough to continue immediately to
  the next planned Salt-family case without reopening the finished Jin work.
- Coordinator note: `codex` is holding `AGENT-050` in the same session because
  the active Salt-family rollout now has a proven long-running package-build
  bottleneck, and the user explicitly requested assessment of safe parallel work
  plus a possible `sbatch` path before broader family rollout continues.
- Coordinator note: `codex` is holding `AGENT-051` in the same session because
  the first Salt 1 Kirst full-window build exposed one bounded thermal-kernel
  blocker and the autonomous queue explicitly requires a narrow remediation pass
  before the rollout can return to its reviewer gate.
- Coordinator note: `codex` is holding `AGENT-052` in the same session because
  the thermal-kernel remediation landed immediately adjacent to the Salt 1
  Kirst package build and the autonomous queue requires the reviewer gate before
  family rollout advances again.
- Coordinator note: `codex` is holding `AGENT-053` in the same session because
  the Salt 1 Kirst reviewer gate is now clear and the autonomous queue advances
  directly into the next planned Salt-family case.
- Coordinator note: `codex` is holding `AGENT-054` in the same session because
  the Salt 2 Jin package finished after the `AGENT-053` build note went stale,
  and the user explicitly requested the pending reviewer gate plus shared-state
  cleanup before the rollout advances again.
- Coordinator note: `codex` is holding `AGENT-055` alongside the already-open
  `AGENT-056` session because the user explicitly requested that the
  self-prompting Salt-family rollout continue through the autonomous queue, and
  the implementation scope is otherwise disjoint from the postprocessing
  campaign work.
- Coordinator note: `codex` is holding `AGENT-060` in the same session because
  the Salt 2 Kirst package completed immediately after `AGENT-055`, and the
  autonomous queue requires the reviewer gate before family expansion advances
  again.
- Coordinator note: `codex` is holding `AGENT-061` alongside the postprocessing
  queue work because the user explicitly requested that the Salt-family rollout
  continue autonomously, and this implementation scope is limited to the shared
  case-profile file plus a new task-scoped package output directory.
- Coordinator note: `codex` is holding `AGENT-064` in the same session because
  Salt 3 Jin finished immediately after `AGENT-061`, and the autonomous queue
  requires the reviewer gate before family expansion advances again.
- Coordinator note: `codex` is holding `AGENT-065` alongside the postprocessing
  queue work because Salt 3 Jin cleared review and the user explicitly
  requested that the Salt-family rollout continue through the remaining queue
  without waiting for another prompt.
- Coordinator note: `codex` is holding `AGENT-066` in the same session because
  Salt 3 Kirst finished immediately after `AGENT-065`, and the autonomous
  queue requires the reviewer gate before family expansion advances again.
- Coordinator note: `codex` is holding `AGENT-067` alongside the postprocessing
  queue work because Salt 3 Kirst cleared review and the user explicitly
  requested that the Salt-family rollout continue through the remaining queue
  without waiting for another prompt.
- Coordinator note: `codex` is holding `AGENT-063` alongside `AGENT-061` because
  the user explicitly requested a bounded refresh of the June 9 heat-audit
  figures, and the implementation scope is limited to the audit builder, its
  generated report package, and task-scoped coordination notes.
- Update the `File ownership` column before editing shared areas.
- Move tasks to `Done Awaiting Review` when work is ready for a reviewer.
