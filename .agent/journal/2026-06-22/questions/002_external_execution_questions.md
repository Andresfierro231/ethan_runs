# External Execution Questions

Date: `2026-06-22`

1. Should the `Fluid` refresh stay a report-only replay against the current
   June 19 diagnostic bundle, or should it also refresh the tracked
   `ethan_cfd_informed_salt_v1` validation snapshot?

2. If the `Fluid` replay is rerun, should the output remain under the existing
   `results/diagnostics/ethan_cfd_informed_salt_v1/` root or land in a new
   dated replay root to preserve June 19 provenance intact?

3. For the LitRev promotion, is the current chapter-level extraction
   sufficient to mark the mixed-convection rows as directly inspected in the
   data-first CSV layer, or should the final pass reopen the original PDFs for
   exact equation/page confirmation before closing the task?
