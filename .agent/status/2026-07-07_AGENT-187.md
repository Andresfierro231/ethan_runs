# AGENT-187 Status

Date: `2026-07-07`
Role: Implementer
Owner: claude

## Scope

Build the F4 buoyancy-friction calibration dataset from mainline Salt 2/3/4
Jin momentum budget; fit a leg-class-specific multiplier (f/f_lam = a + b/Re);
implement `F4_leg_class` closure in `friction_closures.py`; write calibration
script, work products, and documentation.

## Completed

- Read pre-edit protocol files: AGENTS.md, BOARD.md, FILE_OWNERSHIP.md, ROLES.md,
  AGENT-184 attack plan (work_products/2026-07-07_pressure_decomposition_f4_queue/README.md)
- Loaded and parsed momentum_budget.json (18 leg records, Salt 2/3/4 Jin)
- Loaded segment arc lengths from segment_friction.csv
- Built calibration table: 18 admitted rows (3 cases × 6 spans); Salt 1 and
  corrected Q perturbations excluded per admission policy
- Computed x_plus and F3_hagenbach ratio per row
- Fit f_corrected/f_lam = a + b/Re per leg class using OLS normal equations
  (no external dependency):
    - heater (lower_leg):    a=3.113, b=-31.82,   R²=0.876, n=3
    - cooler (upper_leg):    a=2.549, b=-20.12,   R²=0.981, n=3
    - downcomer (right_leg): a=4.361, b=-134.05,  R²=0.991, n=3
    - upcomer (3 spans):     a=1.548, b=-19.62,   R²=0.015, n=9  (POOR)
- Wrote work products in work_products/2026-07-07_f4_buoyancy_friction/:
    - f4_calibration_dataset.csv (18 rows, all computed columns)
    - f4_fit_summary.json (machine-readable coefficients + metadata)
    - f4_fit_summary.csv
    - run_f4_calibration.py (standalone reproducible calibration script)
    - README.md (scientific documentation)
    - summary.json (repo-standard package metadata)
- Added F4_leg_class closure to friction_closures.py AFTER AGENT-186's
  F3_shah_apparent form (coordination respected, no existing forms removed):
    - F4_LEG_CLASS_FITS dict with hard-coded OLS coefficients
    - dp_F4_leg_class() function with leg_class kwarg
    - Updated AVAILABLE_FORMS to include "F4_leg_class"
    - Updated FORM_DESCRIPTIONS
    - Updated compute_dp() to accept and pass **kwargs (backward-compatible)
- Verified: AVAILABLE_FORMS contains F4_leg_class; dp_F4_leg_class produces
  physically plausible values (f/f_lam 2.3–2.9 for heater/cooler/downcomer)
- Verified backward compatibility: F1, F3_hagenbach, F3_hagenbach_always,
  F3_shah_apparent all still work without kwargs
- Ran full test suite: 217 tests pass (no regressions)

## Current State

All deliverables complete. friction_closures.py registered with F4_leg_class.
Calibration CSVs and documentation written. BOARD.md row updated to COMPLETE.

## Follow-up

- Ri-corrected F4 requires extracting wall-bulk ΔT per leg from
  sample_segment_htc_uaprime.py — this is a separate future task
- Upcomer R²=0.015 is a known limitation; the three upcomer sub-legs should
  not be modeled as a single class; sub-leg coefficients would require more
  admitted data points
- Wire F4_leg_class into solver.py ScenarioConfig (separate task; solver.py
  currently only accepts friction_form="F1"/"F3_hagenbach"; needs leg_class
  routing from segment_config)
- Add corrected Salt Q rows once formal gate issues requalified verdict
