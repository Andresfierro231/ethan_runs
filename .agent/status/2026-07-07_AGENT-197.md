# AGENT-197 Status

Date: `2026-07-07`
Role: Implementer
Owner: claude

## Scope

Fix Shah entry flag bug in `build_pressure_term_ledger.py`: the script called
`dp_F3_shah_apparent(..., is_segment_entry=True)` for every span unconditionally,
including `test_section_span` and `left_upper_leg` which receive already-developed
inflow from `left_lower_leg`. This was physically incorrect and caused
`dev_frac > 1.0` for Salt 3/4 on `left_upper_leg`.

The three contiguous upcomer sub-spans run in series:
`left_lower_leg` → `test_section_span` → `left_upper_leg`

Only `left_lower_leg` gets fresh (flat-profile) entry after the lower-left bend.
The subsequent two spans should have `development_loss_pa = 0.0`.

Allowed edit paths: `tools/analyze/build_pressure_term_ledger.py`,
`tools/analyze/test_pressure_term_ledger.py`,
`work_products/2026-07-07_pressure_term_ledger/**`, `.agent/BOARD.md` (own row),
`.agent/status/2026-07-07_AGENT-197.md`,
`.agent/journal/2026-07-07/implementer-pressure-ledger-entry-fix.md`.

## Completed

- Added `SPAN_IS_ENTRY: Dict[str, bool]` dict at module level in
  `build_pressure_term_ledger.py`. `test_section_span` and `left_upper_leg` → False;
  `lower_leg`, `right_leg`, `left_lower_leg`, `upper_leg` → True.
- Modified `compute_development_loss()` to accept `span_name: str = ""` parameter.
  Returns `0.0` immediately when `SPAN_IS_ENTRY.get(span_name, True)` is False.
  No Shah computation for non-entry spans.
- Updated call site in `build_ledger()`: `compute_development_loss(..., span_name=span)`.
- Added `flow_reset_flag` field to the row dict and to `CSV_COLUMNS`.
  True = fresh entry (Shah applicable), False = developed inflow (dev loss = 0.0).
- Updated README text: new `flow_reset_flag` column description; updated caveat 2
  to explain the entry/non-entry split; updated `development_loss_pa` column description.
- Regenerated `pressure_term_ledger.csv`, `pressure_term_ledger.json`, `summary.json`,
  `README.md` under `work_products/2026-07-07_pressure_term_ledger/`.
- Updated `test_pressure_term_ledger.py`: added `flow_reset_flag` to `REQUIRED_COLUMNS`;
  added `NON_ENTRY_SPANS` and `ENTRY_SPANS` constants; added Test 10
  (`development_loss_pa == 0.0` for non-entry spans) and Test 11 (`flow_reset_flag`
  correctness — False for the two non-entry spans, True for the four entry spans).

## Current State

Complete. All 299 tests pass (11 in `test_pressure_term_ledger.py`, 288 in the rest
of the `tools/analyze/test_*.py` and `tools/extract/test_*.py` suites). No regressions.

Verification: Salt 4 `left_upper_leg` `development_loss_pa = 0.0`, `flow_reset_flag = False`.
Salt 2/3/4 `test_section_span` `development_loss_pa = 0.0`, `flow_reset_flag = False`.
All four outer legs have `flow_reset_flag = True` and non-zero `development_loss_pa`.

## Commands Run

```bash
python tools/analyze/build_pressure_term_ledger.py
python -m pytest tools/analyze/test_pressure_term_ledger.py -v   # 11 passed
python -m pytest tools/analyze/test_*.py tools/extract/test_*.py -q  # 299 passed
```

## Files Changed

- `tools/analyze/build_pressure_term_ledger.py`
- `tools/analyze/test_pressure_term_ledger.py`
- `work_products/2026-07-07_pressure_term_ledger/pressure_term_ledger.csv`
- `work_products/2026-07-07_pressure_term_ledger/pressure_term_ledger.json`
- `work_products/2026-07-07_pressure_term_ledger/summary.json`
- `work_products/2026-07-07_pressure_term_ledger/README.md`
