# AGENT-291 Status

Role: Coordinator / Implementer / Tester / Writer
Date: `2026-07-13`
Status: `complete`

## Scope

Plan and launch the Salt2 fine reconstructed-`T` repair continuation needed to
unblock thermal UA/HTC/Nu mesh-family work. Edit scope is limited to the
AGENT-291 row, this status, journal/import/operational note, one focused edit
to `tools/analyze/build_reconstructed_t_repair_trial.py`, and the AGENT-291
work product package.

## Work So Far

- Read root and tools instructions, active board, ownership, roles, AGENT-267
  repair notes, and AGENT-284 mesh/GCI blocker note.
- Added fine-only and package-dir controls to the AGENT-267 reconstructed-`T`
  repair workflow so the fine run does not overwrite the medium package.
- Added longer per-command timeout controls for compute-node execution.
- Created the AGENT-291 plan and sbatch wrapper.

## Runtime Estimate

Estimated fine-only runtime: `3-6 hours`; submitted walltime: `12:00:00`.

## Validation

- `python3.11 -m py_compile tools/analyze/build_reconstructed_t_repair_trial.py`

## Scheduler

- Direct `sbatch` from the current compute-node shell was refused with
  `sbatch not available on compute nodes. Use a login node.`
- Submitted from `login3`.
- Slurm job: `3293768`
- Initial snapshot: `PENDING`, reason `(Resources)`, walltime `12:00:00`.
- Latest snapshot: `RUNNING` on `c318-016`, elapsed `1:03` at check time,
  walltime `12:00:00`.
- Harvest command:

```bash
sacct -j 3293768 --format=JobID,JobName,State,ExitCode,Elapsed,NodeList%30
```

## Terminal Closeout

Terminal Slurm evidence:

```text
3293768      s2_fine_T+  COMPLETED      0:0   00:22:33        c318-016
3293768.bat+      batch  COMPLETED      0:0   00:22:33        c318-016
3293768.0    python3.11  COMPLETED      0:0   00:22:32        c318-016
```

Fine repair output package:

- `work_products/2026-07/2026-07-13/2026-07-13_salt2_fine_reconstructed_t_repair_plan_sbatch/repair_trial_output/summary.json`

The fine repair smoke passed clean reconstructed `T`, temperature section
sampling, and segment thermal extraction. This closes the missing-fine-thermal
extraction blocker, but it does not admit thermal UA/HTC/Nu to closure fitting.
The follow-up gate is documented under `TODO-PRED-THERMAL-MESH-GATE`.
