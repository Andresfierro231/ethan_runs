# AGENT-155 Status

- Task: repair the Salt 4 latest-window failure before rerunning the queue
  chain.
- Role: Implementer / Coordinator
- Started: `2026-06-30T10:00:00-05:00`
- Paths:
  - `.agent/BOARD.md`
  - `.agent/status/2026-06-30_AGENT-155.md`
  - `.agent/journal/2026-06-30/implementer-latest-window-salt4-failure-repair.md`
  - `tools/extract/sample_leg_centerline_major_loss.py`
  - `tmp/2026-06-23_ethan_latest_window_case_analysis_refresh/**`
  - `tmp_extract/ethan_streamwise_friction/viscosity_screening_salt_test_4_jin_coarse_mesh/**`
  - `journals/2026-06/2026-06-30_ethan_runs.md`

## Completed

- Traced the Salt 4 failure to stale reuse logic in
  `tools/extract/sample_leg_centerline_major_loss.py`: the extractor was
  trusting an existing `thermal_sanitization_summary.json` and skipping a
  fresh scan of reconstructed retained `T` fields.
- Updated `sanitize_reconstructed_thermal_fields` so the selected retained
  times are always rescanned and the sanitization summary is always rewritten.
- Quarantined the stale streamwise-friction cache root
  `tmp_extract/ethan_streamwise_friction/viscosity_screening_salt_test_4_jin_coarse_mesh/ad9ca25a2259410c`
  to
  `tmp_extract/ethan_streamwise_friction/viscosity_screening_salt_test_4_jin_coarse_mesh/ad9ca25a2259410c_stale_20260630T100000`
  before rebuilding.
- Confirmed the fresh cache rewrote
  `thermal_sanitization_summary.json` with replacements at the formerly bad
  retained times including `8110`, `8111`, and `8123`.
- Ran a local Salt 4 rebuild far enough to confirm the repaired
  `foamPostProcess` path advanced through retained times `8104-8106` without
  reproducing the previous fatal `T`-field read. Stopped that local validation
  before completion so the repaired Slurm chain could reuse the same cache
  without collision.
- Handed the fix back to the queue lane, which resubmitted the repaired chain
  beginning with `3267436` `lw_s234_full`.

## Validation

- `python -m py_compile tools/extract/sample_leg_centerline_major_loss.py`
- `sed -n '1,260p' tmp_extract/ethan_streamwise_friction/viscosity_screening_salt_test_4_jin_coarse_mesh/ad9ca25a2259410c/thermal_sanitization_summary.json`
- `ps -ef | rg "build_ethan_case_analysis_package.py --source-id viscosity_screening_salt_test_4_jin_coarse_mesh|foamPostProcess -case .*ad9ca25a2259410c|sample_leg_centerline_major_loss.py --source-id viscosity_screening_salt_test_4_jin_coarse_mesh"`

## Remaining

- Monitor `3267436` for a full queued rebuild of `salt4_cont` and confirm the
  downstream chain advances off the repaired output tree.
