# AGENT-185 Status

Date: `2026-07-07`
Role: Implementer
Owner: claude

## Scope

Fix the corrected-Salt status monitoring script (`collect_corrected_salt_status.py`):
- Replace the fixed 200-sample late window with a time-based 5-minute (300 s) window
- Verify the monitor name and confirm it exists in all 14 cases
- Manually verify the direction-check logic against raw surfaceFieldValue.dat data
- Write an independent verification script (zero shared code) to confirm direction check
- Document sign convention, caveats, and manual cross-check instructions

## Completed

- **Monitor name confirmed**: `mdot_pipeleg_lower_05_straight` exists in all 14 cases
- **Sign convention confirmed**: `sum(phi)` is always negative (face normal opposes primary flow); taking `abs()` is correct and does not discard physical information
- **Direction check verified manually** for all 14 cases by direct inspection of `surfaceFieldValue.dat`:
  - All 14 cases moving in the expected direction (Q^(1/3) scaling)
  - Actual moves range from 1.6% to 7.0% in absolute terms, all in the correct direction
- **`collect_corrected_salt_status.py` rewritten** with:
  - `LATE_WINDOW_DURATION_S = 300.0` constant (5 min of simulated time)
  - `detect_write_interval()` helper (median dt from first 20 consecutive steps)
  - Time-based late window: `t_cutoff = latest_t - 300.0` with graceful fallback
  - New CSV fields: `write_interval_s`, `late_window_duration_s`, `late_window_n_samples`
  - Comprehensive module docstring: direction-check logic, sign convention, caveats, verification approach
  - Advancement threshold: 1000 s after restart flagged as a positive signal (not proof of success)
- **Independent verification script** created at `work_products/2026-07-07_collect_salt_status_verification/verify_direction_logic.py`:
  - Zero shared code with `collect_corrected_salt_status.py`
  - Reads every `surfaceFieldValue.dat` directly
  - Per-case table with `q_ratio`, `expected_pct`, `first_m`, `latest_m`, `actual_pct`, `n_records`, `t_span_s`, `direction_ok`, `verdict`
  - PASS / FAIL / INSUF_DATA / NO_DATA verdicts
  - Sign convention check
  - Writes `direction_check_verification.csv`
- **Verification script run** — confirmed 14/14 PASS, all first_m negative

## Current State

AGENT-185 is COMPLETE. All 14 corrected Salt Q cases pass the direction check:
- PASS=14, INSUF_DATA=0, FAIL=0, NO_DATA=0
- All `first_m` values negative (sign convention correct)
- Results written to `work_products/2026-07-07_collect_salt_status_verification/direction_check_verification.csv`

## Follow-up

- The `needs_special_gate_scrutiny` flag in `collect_corrected_salt_status.py` remains: no flagged row is closure-fit admissible without coordinator review
- Salt 3 hi10q and hi5q have only 20-22 records (flagged INSUF_DATA by the independent script); these are early in their run and will accumulate more data
- Formal gate (job 3279638/3279646) still pending solver job completion (~2 days); these scripts will be rerun after gate jobs finish
- Jin gap analysis (PID 94224) still running; outputs expected ~15:15 CDT 2026-07-07
