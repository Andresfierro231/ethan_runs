# AGENT-102 Status

- Role: `Implementer / Writer`
- Status: active
- Scope:
  - `.agent/BOARD.md`
  - `.agent/status/2026-06-22_AGENT-102.md`
  - `.agent/journal/2026-06-22/questions/**`
  - `.agent/journal/2026-06-23/questions/**`
  - `.agent/journal/2026-06-29/track-a_AGENT-102_v2-refresh-and-replay.md`
  - `tools/analyze/build_ethan_fluid_replay_against_frozen_state.py`
  - `tools/analyze/test_ethan_fluid_replay_against_frozen_state.py`
  - `reports/2026-06/2026-06-22/2026-06-22_ethan_fluid_replay_against_frozen_state/**`
  - `../cfd-modeling-tools/tamu_first_order_model/Fluid/**`

## Current progress

- Refreshed the local replay builder to follow the nested June 22 report roots
  and regenerated:
  `reports/2026-06/2026-06-22/2026-06-22_ethan_fluid_replay_against_frozen_state/`
- Extended the local replay audit so it now distinguishes:
  - resolved external `v2` bundle publication
  - resolved loader/config/test wiring
  - resolved reproducible bundle producer
  - still-open readable `v2` diagnostics
  - still-open carried-forward `v1` surface tables
- Landed the external parallel `v2` refresh path in
  `../cfd-modeling-tools/tamu_first_order_model/Fluid/**`:
  - `validation_data/ethan_cfd_informed_salt_v2/`
  - `tools/build_ethan_cfd_informed_salt_v2_bundle.py`
  - matching `campaigns.yaml`, `scenarios.yaml`, and tests
- Fixed an external config regression where four legacy `v1` scenarios had been
  accidentally absorbed into the new `v2` campaign definition.
- Started the bounded readable rerun:
  `python -m tamu_loop_model_v2.run_resumable --campaign ethan_cfd_informed_salt_v2 --output-root results/diagnostics/ethan_cfd_informed_salt_v2 --output-label ethan-cfd-informed-salt-v2 --jobs 2`
  and confirmed live worker processes plus partial scenario manifests.

## Main findings so far

- Straight sections are not treated as automatically fully developed.
- Direct internal `Nu` remains defended only on `left_lower_leg`.
- `upcomer` remains sensitivity-only and should be modeled separately from the
  direct straight branches.
- `right_leg` / downcomer remains blocked for direct `Nu`.
- The present domain-breadth gap is concrete:
  readable hybrid scenarios exist only for Salt 1, so baseline-vs-hybrid
  closure conclusions are under-supported across the rest of the Salt family.
- The external blocker narrowed materially on June 29:
  the tracked `Fluid` bundle is no longer stuck on `v1`, but the rerun is not
  yet finalized and the `v2` static bundle still carries forward descriptor and
  safe-subset surface tables unchanged from `v1`.

## Current state note

- The local replay package is current against the June 22 frozen-state
  contract.
- The readable external `Fluid` campaign on disk is still not finalized:
  `results/diagnostics/ethan_cfd_informed_salt_v2/run_manifest.csv` is still
  absent, but the original June 29 worker family (`2642865`, `2642940`,
  `2642943`) is still running and advancing the partial scenario manifests.
- The external `Fluid` repo now has a parallel `v2` bundle, campaign, tests,
  and producer path, so the remaining gap is the live rerun plus the later
  dedicated surface-table refresh.
- The current presentation bakeoff now states the CFD-vs-1D basis explicitly,
  but it still inherits the legacy readable external `Fluid` scenario surface.
- Concretely, there is still no published global `1.4 in` Salt scenario inside
  the readable external bundle; the defended winner remains the baseline
  `1.0 in` radiation-on scenario, while only the hybrid rows expose branchwise
  effective insulation multipliers such as `right_vertical = 1.40`.

## Verification

- `python3.11 -m py_compile tools/analyze/build_ethan_fluid_replay_against_frozen_state.py tools/analyze/test_ethan_fluid_replay_against_frozen_state.py`
- `python3.11 -m unittest tools.analyze.test_ethan_fluid_replay_against_frozen_state -q`
- `python3.11 -m tools.analyze.build_ethan_fluid_replay_against_frozen_state`
- `python -m unittest tests.test_ethan_cfd_informed_salt tests.test_profile_descriptor_closure`
- `python -m tamu_loop_model_v2.run_resumable --campaign ethan_cfd_informed_salt_v2 --output-root results/diagnostics/ethan_cfd_informed_salt_v2 --output-label ethan-cfd-informed-salt-v2 --jobs 2`

## Next step

- Test a split resumable relaunch path for `ethan_cfd_informed_salt_v2`:
  if the current overnight replay still fails to finish,
  - confirm `prepare` / `solve-scenario` / `finalize` behavior on one scenario
    first
  - then run remaining heavy hybrid scenario lanes in parallel rather than
    repeating a single `--jobs 2` campaign
  - regenerate the June 22 local replay package only after a finalized
    `run_manifest.csv` exists
