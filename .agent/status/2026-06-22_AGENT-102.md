# AGENT-102 Status

- Role: `Implementer / Writer`
- Status: active
- Scope:
  - `.agent/BOARD.md`
  - `.agent/status/2026-06-22_AGENT-102.md`
  - `.agent/journal/2026-06-22/questions/**`
  - `.agent/journal/2026-06-23/questions/**`
  - `tools/analyze/build_ethan_fluid_replay_against_frozen_state.py`
  - `tools/analyze/test_ethan_fluid_replay_against_frozen_state.py`
  - `reports/2026-06-22_ethan_fluid_replay_against_frozen_state/**`
  - `../cfd-modeling-tools/tamu_first_order_model/Fluid/**`

## Current progress

- Built the local replay package:
  `reports/2026-06-22_ethan_fluid_replay_against_frozen_state/`
- Added and validated the local builder plus unit test coverage:
  - `tools/analyze/build_ethan_fluid_replay_against_frozen_state.py`
  - `tools/analyze/test_ethan_fluid_replay_against_frozen_state.py`
- Published the current best readable 1D-vs-frozen-CFD rows without claiming a
  new June 22 external rerun.
- Confirmed the exact external blocker:
  - the tracked external Fluid surface is still
    `validation_data/ethan_cfd_informed_salt_v1/`
  - that bundle snapshot is still the older June 19 science
  - no producer script currently exists under
    `../cfd-modeling-tools/tamu_first_order_model/Fluid/**` to rebuild a
    refreshed Ethan CFD-informed Salt validation bundle from the June 22 local
    closure/report roots
- Verified the requested cleanup:
  - `find ../cfd-modeling-tools/tamu_first_order_model/Fluid -type f \( -name '*.out' -o -name '*.sbatch' \) | wc -l`
    now returns `0`

## Main findings so far

- Straight sections are not treated as automatically fully developed.
- Direct internal `Nu` remains defended only on `left_lower_leg`.
- `upcomer` remains sensitivity-only and should be modeled separately from the
  direct straight branches.
- `right_leg` / downcomer remains blocked for direct `Nu`.
- The present domain-breadth gap is concrete:
  readable hybrid scenarios exist only for Salt 1, so baseline-vs-hybrid
  closure conclusions are under-supported across the rest of the Salt family.

## Stale note

- The local replay package is current against the June 22 frozen-state
  contract.
- The readable external `Fluid` campaign is stale relative to that contract
  because it still consumes the June 19 `v1` bundle and there is no in-repo
  refresh producer yet.
- The current presentation bakeoff now states the CFD-vs-1D basis explicitly,
  but it still inherits the stale external `Fluid` scenario menu.
- Concretely, there is still no published global `1.4 in` Salt scenario inside
  the readable external bundle; the defended winner remains the baseline
  `1.0 in` radiation-on scenario, while only the hybrid rows expose branchwise
  effective insulation multipliers such as `right_vertical = 1.40`.

## Verification

- `python3.11 -m py_compile tools/analyze/build_ethan_fluid_replay_against_frozen_state.py tools/analyze/test_ethan_fluid_replay_against_frozen_state.py`
- `python3.11 -m unittest tools.analyze.test_ethan_fluid_replay_against_frozen_state -q`
- `python3.11 -m tools.analyze.build_ethan_fluid_replay_against_frozen_state`

## Next step

- Claim the next bounded external `Fluid` refresh task and either:
  - add a reproducible producer for a refreshed Ethan CFD-informed Salt bundle,
    then rerun the tracked campaign, or
  - stage a versioned `v2` static bundle plus matching scenario/campaign
    wiring if a producer cannot be landed quickly enough for the immediate
    presentation window.
