# AGENT-290 Status

Date: `2026-07-13`
Role: Coordinator / Implementer / Tester / Writer
Status: complete; repaired follow-up job `3293924` running

## Scope

Repair selected corrected Salt-Q continuation launch after `3293441.0`-`.3`
failed on OpenFOAM time-name precision.

## Changes

- Extended `tools/analyze/check_corrected_salt_preflight.py` to validate
  runtime controls:
  - expected restart time per case;
  - root and restart Q patch consistency;
  - `startFrom startTime` for explicit integer restart repairs;
  - `timeFormat general` for the July 10 integer-restart repair pattern;
  - exact `timePrecision 6` for the selected rerun contract.
- Updated
  `jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/scripts/run_selected_corrected_salt_q_continuation.sbatch`
  to:
  - patch explicit integer restart fields (`10291`, `9674`, `11655`, `12639`);
  - require the preflight audit before launching `foamRun`;
  - use `startFrom startTime`, `timeFormat general`, and `timePrecision 6`.
- Patched the four selected staged `system/controlDict` files to the same
  explicit integer-start/general-format contract.
- Documented the failure mode and guardrail in the campaign README/TODO and
  submission CSV.

## Validation

- `python3.11 -m py_compile tools/analyze/check_corrected_salt_preflight.py`
- Focused manual invocation of 7 pytest-style preflight tests using temporary
  directories.
- `bash -n jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/scripts/run_selected_corrected_salt_q_continuation.sbatch`
- Local four-case runtime/Q preflight:
  `Audited 4 corrected Salt cases; failures=0`

## Scheduler State

- `3293441`: failed under `timeFormat general`, `timePrecision 12`.
- `3293739`: failed under `timeFormat general`, `timePrecision 17`; OpenFOAM
  attempted precision `18` and hit its maximum.
- `3293765`: failed under `timeFormat fixed` with `startFrom startTime`;
  OpenFOAM could not find restart object `T` because the fixed-formatted
  start-time name did not match the existing integer restart directory.
- `3293782`: failed under `timeFormat fixed` with `startFrom latestTime`;
  OpenFOAM still could not find restart object `T` because the fixed-format
  time name did not match the integer restart directory.
- `3293924`: submitted at `2026-07-13T17:03:55-05:00`; `RUNNING` on
  `c318-016` after the initial monitoring window. At `00:02:29` elapsed for
  each `foamRun` step, `3293924.0`-`.3` were all `RUNNING` and each case log
  had advanced beyond the integer restart without the earlier time-name fatal
  error.
