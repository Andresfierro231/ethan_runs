---
provenance:
  - work_products/2026-07/2026-07-17/2026-07-17_wall_test_section_distribution_ladder/README.md
tags: [status, AGENT-498, wall-circuit, test-section, heat-placement]
related:
  - predictive-wall-test-section-submodels
  - .agent/BOARD.md
task: AGENT-498
date: 2026-07-17
role: Implementer/Tester/Writer
type: status
status: complete
---
# AGENT-498 Status

Implementation and coupled Fluid scoring are complete.

- Output package:
  `work_products/2026-07/2026-07-17/2026-07-17_wall_test_section_distribution_ladder/`
- Package complete: `12` scenario contracts, `2` local wall candidates, and
  `4` coupled wall+cooler candidates.
- Static/runtime prechecks pass for both local wall candidates.
- Latest coupled Fluid scoring run: foreground compute-node replay with
  `srun -N1 --overlap -n1 python3 tools/analyze/build_wall_test_section_distribution_ladder.py --run-fluid --timeout-seconds 273`.
  The first non-overlap `srun` attempt could not create a job step because the
  allocation was busy and was cancelled before writing scoring output. The
  scorecard was refreshed from the completed rows with `--reuse-existing-coupled`.
- Coupled rows: `12/12` completed with accepted roots.
- Probe evidence: `204` coupled probe localization rows, `136`
  validation/holdout probe delta rows, and `80` role/segment summaries. Probe
  gates are `106` fail, `14` pass, and `16` not-compared rows where M3 has no
  sensor prediction.
- Coupled admission: `0/4` candidates admitted.

The package tests a local distribution ladder instead of another passive-total
hA cancellation pass. `PB2_salt2_local_shape_passive_hA_p1` redistributes
passive hA by a Salt2-trained segment/role shape. `PB3_upcomer_test_section_attenuated_shape_p1`
adds Salt2 wall-layer-derived attenuation on upcomer ambient/test-section roles.

Decision remains `keep_open` for `predictive-wall-test-section-submodels`.
Both PB2 and PB3 improve mdot error versus M3 on Salt3/Salt4, but all-probe and
TW RMSE remain far worse than M3. Best observed all-probe rows are still about
`53.21 K` on Salt3 and `58.22 K` on Salt4, compared with M3 `17.749 K` and
`16.971 K`. Probe localization puts the largest compared regressions on Salt4
TW heated-incline sensors, especially TW5/TW6.

## Validation

- `python3 tools/analyze/test_wall_test_section_distribution_ladder.py`
- `srun -N1 --overlap -n1 python3 tools/analyze/build_wall_test_section_distribution_ladder.py --run-fluid --timeout-seconds 273`
- `python3 tools/analyze/build_wall_test_section_distribution_ladder.py --reuse-existing-coupled --timeout-seconds 273`
