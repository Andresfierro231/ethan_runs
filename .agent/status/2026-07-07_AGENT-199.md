# AGENT-199 Status

Date: `2026-07-07`
Role: Implementer
Owner: claude

## Scope
Add the four missing horizontal manifold stub segments (`top_horizontal_inlet`,
`top_horizontal_exit`, `bottom_horizontal_inlet`, `bottom_horizontal_exit`) to
`_F4_LEG_CLASS_BY_PARENT_SEGMENT` in
`../cfd-modeling-tools/tamu_first_order_model/Fluid/tamu_loop_model_v2/solver.py`.
Add a corresponding test class `TestF4StubMapping` in
`../cfd-modeling-tools/tamu_first_order_model/Fluid/tests/test_friction_closures.py`.

## Completed

- Pre-edit protocol: read AGENTS.md, BOARD.md (AGENT-199 row confirmed), FILE_OWNERSHIP.md, ROLES.md.
- Verified segment names via `grep` on geometry.py and loop_segments.csv: names confirmed as
  `top_horizontal_inlet`, `top_horizontal_exit`, `bottom_horizontal_inlet`, `bottom_horizontal_exit`.
- Noted discrepancy between task brief leg_class assignments and AGENT-195 runtime patch values
  (see journal for full analysis). Adopted AGENT-195 values (downstream-classification principle)
  as physically more consistent and already validated in the comparison run.
- Edited `solver.py` lines 526-535: added 4 stub entries with inline flow-direction comments.
- Edited `tests/test_friction_closures.py`: added `from unittest.mock import MagicMock` and
  `from tamu_loop_model_v2.solver import _F4_LEG_CLASS_BY_PARENT_SEGMENT, _friction_closure_kwargs_for_segment`
  imports; added `TestF4StubMapping` class with 7 tests.
- All tests pass: 52/52 in `test_friction_closures.py`, 299/299 in ethan_runs suite.

## Current State

Solver.py now maps all 12 parent segments (8 original + 4 stub) without raising ValueError
when F4_leg_class is used with any geometry segment. The AGENT-195 runtime patch in
`run_mdot_comparison.py` is now superseded by the canonical solver.py entry.

## Follow-up

- AGENT-195's comparison script (`work_products/2026-07-07_friction_forms_comparison/`) patches
  `_F4_LEG_CLASS_BY_PARENT_SEGMENT` at runtime; the runtime patch can be removed since solver.py
  now has the canonical values. The `S._F4_LEG_CLASS_BY_PARENT_SEGMENT.update(...)` call is now
  a no-op (same values) but is harmless.
- All leg_class assignments for stub segments are provisional. If AGENT-200 (F5) finds that
  buoyancy corrections on horizontal stubs matter significantly, revisit these assignments with
  physics-based justification.
- The AGENT-199 brief suggested cooler/cooler/downcomer/heater (inconsistent scheme); the adopted
  values are cooler/downcomer/heater/upcomer (downstream principle). Document this deviation in
  any future review.
