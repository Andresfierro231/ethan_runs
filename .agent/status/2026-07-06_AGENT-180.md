# AGENT-180 Status

Date: `2026-07-06`
Role: Coordinator / Implementer / Writer
Owner: codex

## Scope

Submit and document the requested overnight postprocess/follow-up jobs:

- Water postprocess rerun.
- Corrected Salt Q dependency-gated postprocess/admission gate.
- Jin per-leg gap analysis rerun.

## Current State

Scripts and documentation have been staged under:

- `tmp/2026-07-06_overnight_postprocess_jobs/`
- `work_products/2026-07-06_overnight_postprocess_jobs/`
- `operational_notes/07-26/06/2026-07-06_overnight_postprocess_submissions.md`
- `.agent/journal/2026-07-06/overnight-postprocess-submissions.md`

Submitted jobs:

- `3278452` `water_post_rerun`: pending on resources at submission check.
- `3278453` `saltq_gate_after`: pending on dependency `afterany:3275448:3275449:3275560`.
- `3278454` `jin_gap_rerun`: pending on resources at submission check.

## 2026-07-07 Morning Check

- `3278452` `water_post_rerun`: `COMPLETED` in `00:00:33`, exit `0:0`.
  It wrote July 6 Water status, consolidated closure, and next-model-form
  summaries under
  `work_products/2026-07-06_overnight_postprocess_jobs/water_postprocess_after_3265970/`.
- `3278453` `saltq_gate_after`: still `PENDING` on dependency because parent
  corrected Salt jobs `3275448`, `3275449`, and `3275560` are still running.
- `3278454` `jin_gap_rerun`: `FAILED` in `00:00:04`, exit `1:0`. The import
  path issue was fixed, but the copied script now fails against the current
  Fluid scenario API with
  `TypeError: __init__() got an unexpected keyword argument 'use_radiation'`.
- Corrected Salt parents are still live and producing solver progress:
  `3275448` and `3275449` have run about three days; `3275560` has run about
  two days and twenty-two hours. Targeted log-tail checks found no fatal
  markers in current tails. Preflight patch audits exist for the active cases
  and report `root_patch_ok=True`, `processor_patch_ok=True`.
- Latest observed corrected-Salt log times span roughly `4010.590361446s`
  (`salt1_jin_hi10q_corrected`, completed one `foamRun` step) through
  `11060.616161616s` (`salt4_jin_lo5q_corrected`). Most Salt2/3/4 corrected
  cases are beyond `8700s`; `salt1_jin_lo10q_corrected` is at about `5409s`.

## 2026-07-07 Coordinator Follow-up

- Fixed the Salt gate scheduler partition: patched
  `tmp/2026-07-06_overnight_postprocess_jobs/run_corrected_salt_postprocess_afterany.sbatch`
  from `development` to `NuclearEnergy`.
- Submitted replacement dependency job `3279638` with
  `afterany:3275448:3275449:3275560`.
- Canceled stale development job `3278453` to avoid duplicate gate execution.
- Verified queue state: `3279638` is pending on dependency in `NuclearEnergy`;
  parent jobs `3275448`, `3275449`, and `3275560` are running in
  `NuclearEnergy`.
- Wrote coordinator status and task queue:
  `operational_notes/07-26/07/2026-07-07_coordinator_status_and_task_queue.md`.
- Live mdot sanity check shows every corrected active Salt Q case moving in the
  expected direction from restart. This confirms the intended heat-input
  perturbations are affecting the runs. Rows remain inadmissible until the
  operating-point gate confirms a re-plateau; `salt1_jin_hi10q_corrected` needs
  special scrutiny because it ended after only about `254 s` past restart.
- Updated `collect_corrected_salt_status.py` so the pending gate writes
  advancement, mdot-response, late-window, and `needs_special_gate_scrutiny`
  columns. Smoke test in `/tmp/agent180_collect_smoke` flagged
  `salt1_jin_hi10q_corrected` with reason: solver ended before enough
  post-restart advancement (`<1000 s` or `<20%` of target extension).

## Morning Checks

Use `sacct` on the submitted job IDs, then read the matching Slurm stdout/stderr logs. For corrected Salt, only rows with `operating_point_verdict=requalified` should be admitted into closure or model fitting.

## Validation

- `bash -n` passed for all three sbatch wrappers.
- `py_compile` passed for `collect_corrected_salt_status.py` and the copied Jin gap scripts.
- Salt manifest smoke passed with `build_postprocessing_run_status_inventory.py --no-sacct`.
- Water rerun smoke passed and classified `3265970` as `freeze_and_postprocess_water`.
- Queue check after submission showed `3278452` and `3278454` pending on priority, and `3278453` pending on dependency.
