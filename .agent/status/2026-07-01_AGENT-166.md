# AGENT-166 Status

Date: `2026-07-01`
Role: Coordinator / Implementer
Owner: claude

## Scope

T3: implement and run the operating-point (re-equilibration) convergence gate for Salt Q-perturbation runs. Resolves `TODO-PERT-REQUAL`. Allowed paths: `tools/analyze/assess_time_convergence.py` (T3 gate additions), `tools/analyze/test_assess_time_convergence.py` (new), `work_products/2026-07-01_claude_perturbation_requal/**` (new), `.agent/journal/2026-07-01/T3-perturbation-requalification.md`.

## Completed

- Extended `assess_time_convergence.py` with `--require-moved-from` / `--expected-q-ratio` gate: loop mdot must have MOVED from the nominal baseline by ~Q^(1/3) expectation AND re-plateaued. Also reports advance-past-restart and advance/tau. New verdicts: `requalified` / `false_steady` / `insufficient_move` / `moving_not_plateaued` / `too_short`.
- Added `test_assess_time_convergence.py` (7 tests: Q^(1/3) scaling, restart detection, false_steady, requalified, too_short, gate-off).
- Ran gate over all 16 perturbation/control runs. Wrote per-run table (JSON + CSV) and reproducible driver to `work_products/2026-07-01_claude_perturbation_requal/`.
- 183 tests pass (baseline stayed green).

## Current State

COMPLETE. Finding: all 14 genuine Q-perturbation runs are FALSE_STEADY — mdot moved <0.3% from nominal vs a 1.6–3.5% Q^(1/3) expectation. None are usable for the upcomer recirculation correlation. Lane U1 / T10 stays blocked pending T2 true-steady re-runs.

## Follow-up

- Requalification requires the T2 true-steady runs (AGENT-164 / corrected Salt Q perturbation jobs 3275448–3275451).
- Do NOT add any existing Q-perturbation run to a correlation table until the gate returns `requalified`.
