# AGENT-253 Status

Role: Coordinator / Implementer / Tester / Writer

Task: Refresh corrected-Q latest-timestep reporting and attach repaired Salt4 +10Q to an underfilled weekend allocation.

## Outcome

Status: complete.

- Updated `tools/analyze/build_registry_corrected_q_status_table.py` so the corrected-Q tables show:
  - 3280969 gate latest time,
  - latest written timestep scanned from each registered case root,
  - latest live/logged solver time parsed from `logs/log.foamRun*`.
- Regenerated the selected/all corrected-Q tables and added `corrected_q_latest_timesteps.csv`.
- Identified underfilled running allocations:
  - `3288671` selected corrected-Q job had 256 allocated CPUs and only two live 64-core steps after Salt4 failed, leaving about 128 free cores before the repair attach.
  - `3282992` Salt1 nominal had 256 allocated CPUs and one live 64-core step, leaving about 192 free cores, but it is a separate nominal job.
- Chose `3288671` because it is the same corrected-Q campaign allocation and had enough free cores.
- Repaired Salt4 +10Q staged controls to restart explicitly from clean written time `11536` with `timePrecision 6`.
- Added single-row attach launcher `jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/scripts/run_salt4_hi10q_attach_to_allocation.sh`.
- Attached repaired Salt4 +10Q as Slurm step `3288671.5`; it was RUNNING on `c318-017` at final check and had advanced to latest written timestep `11537.000 s` / latest log time `11537.723 s`.

## Repair Attempts

- Original packed step `3288671.2` failed at `00:02:33` with OpenFOAM maximum time precision at `11536.488262910847`.
- Repair attempt `3288671.3` used fixed-format time names and failed because OpenFOAM looked for restart directory `11536.000000000` while the clean restart directory is `11536`.
- Repair attempt `3288671.4` used `timePrecision 16` and failed again at the same OpenFOAM time-precision check.
- Successful live attempt `3288671.5` uses `timeFormat general`, `timePrecision 6`, `startFrom startTime`, `startTime 11536`.

## Validation

- `python3.11 -m unittest tools.analyze.test_registry_corrected_q_status_table` passed.
- `python3.11 tools/analyze/build_registry_corrected_q_status_table.py --strict-registry` passed.
- Attach launcher `bash -n` passed.
- Attach launcher preflight passed.
- `sacct -j 3288671` showed `3288671.5` RUNNING at final check.

## Admission Boundary

Salt4 +10Q corrected remains sensitivity/correlation-support. It is not closure-fit admissible until post-run gate evidence explicitly admits it.
