# AGENT-249 Status

Role: Coordinator / Implementer / Tester / Writer

Task: Check selected corrected Salt Q steadiness and submit only the requested under-advanced rows as one packed one-node continuation.

## Current status

- Selected rows: `salt1_jin_lo10q_corrected`, `salt1_jin_hi10q_corrected`, `salt4_jin_hi10q_corrected`.
- Steadiness verdict: all three still need continuation before any admission; mdot looks good or close, but formal gate verdict is `too_short` and heat/operating-point evidence remains under-advanced.
- Staged input edits completed: selected `endTime`s set to `30000`; selected `timePrecision`s set to `12`; Salt4 +10Q monitor changed to diagnostic-only.
- Salt1 +10Q launch repair completed: final selected launcher patches/restarts it from integer time `4010` after fractional restart `4010.590361446` failed OpenFOAM `T` lookup.
- Validation completed: launcher `bash -n` passed; patcher `py_compile` passed; selected staged functions no longer contain `writeNow` stop controls.
- Final active submission: job `3288671`, one NuclearEnergy node on `c318-017`.
- End-of-day accounting update: `3288671.0` and `3288671.1` remain `RUNNING`; `3288671.2` failed after `00:02:33`.
- Failed step mapping from the end-of-day wrap-up: `3288671.2` corresponds to `salt4_jin_hi10q_corrected` and hit an OpenFOAM maximum time-precision failure at current time name `11536.488262910847`.
- Treat Salt4 +10Q corrected as repair-first. Do not blind-resubmit or infer physical non-convergence from this launch failure.

## Admission boundary

These rows remain `sensitivity/correlation-support`. They are not nominal baselines and are not closure-fit admissible until a post-continuation gate explicitly admits them.
