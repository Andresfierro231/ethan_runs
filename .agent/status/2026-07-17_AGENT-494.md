---
provenance:
  - work_products/2026-07/2026-07-17/2026-07-17_wall_test_section_coupled_admission/README.md
tags: [status, AGENT-494, wall-circuit, test-section, coupled-score]
related:
  - predictive-wall-test-section-submodels
  - .agent/BOARD.md
task: AGENT-494
date: 2026-07-17
role: Implementer/Tester/Writer
type: status
status: complete
---
# AGENT-494 Status

Package implementation and coupled Fluid adjudication are complete.

- Output package:
  `work_products/2026-07/2026-07-17/2026-07-17_wall_test_section_coupled_admission/`
- Coupled rows: `12/12` completed with accepted roots.
- Runtime gate: pass; contracts use setup external-boundary rows, PB1 Salt2
  passive drive, and cooler alpha_UA.
- PB1 passive-total static gate: pass.
- Local TS6/TS7 gate: diagnostic/nonblocking fail on percent thresholds.
- Coupled admission: `0/4` PB1+HX candidates admitted.

Decision remains `keep_open` for `predictive-wall-test-section-submodels`.
Every PB1+HX lumped/segmented candidate improves mdot error vs M3 on Salt3 and
Salt4, but fails the coupled gate because all-probe/TW RMSE is much worse than
M3. Representative validation/holdout all-probe RMSE values are about
`53.29 K` / `60.55 K` for PB1+HX versus M3 `17.749 K` / `16.971 K`.

Scheduler provenance: background job `3300338` was the first submission path;
job `3300339` was the completed `NuclearEnergy-dev` run that produced the final
coupled rows with `--run-fluid --timeout-seconds 273`, then the package was
refreshed with `--reuse-existing-coupled`.

## Validation

- `python3 tools/analyze/build_wall_test_section_coupled_admission.py`
- `python3 -m unittest tools.analyze.test_wall_test_section_coupled_admission`
- `python3 -m py_compile tools/analyze/build_wall_test_section_coupled_admission.py tools/analyze/test_wall_test_section_coupled_admission.py`
- Slurm job `3300339`: `python3 tools/analyze/build_wall_test_section_coupled_admission.py --run-fluid --timeout-seconds 273`
- `python3 tools/analyze/build_wall_test_section_coupled_admission.py --reuse-existing-coupled --timeout-seconds 273`
- `python3 -m json.tool imports/2026-07-17_wall_test_section_coupled_admission.json`

## Continue From Here

Do not close this blocker via another passive-total hA scaling pass. The next
candidate needs local wall-temperature, wall-shape, or test-section
distribution physics that reduces wall/probe RMSE while preserving the mdot
improvement, without using realized CFD wallHeatFlux, CFD mdot, imposed cooler
duty, realized test-section heat, or validation/holdout temperatures as runtime
inputs.
