# AGENT-248 Status

Updated: `2026-07-10T16:48:58-0500`

Role: Coordinator / Implementer / Tester / Writer

## Goal

Prove Salt2 medium/fine refined pressure-only section sampling works on the
current compute node, document the smoke-test evidence, and submit the
`2026-07-10T20:00:00` Slurm batch rerun only if hard gates pass.

## Outcome

Status: immediate compute-node rerun complete; 8pm batch canceled as duplicate.

Smoke tests passed on `c318-008.ls6.tacc.utexas.edu`:

- `python3.11 -m py_compile` passed for the AGENT-248 generator and test.
- Generator contract assertions passed.
- Generated runner and sbatch scripts passed `bash -n`.
- `srun -N1 -n1 -c128 ... run_pressure_extraction.sh preflight` passed.
- `srun -N1 -n1 -c128 ... run_pressure_extraction.sh smoke` passed at
  `2026-07-10T12:06:47-0500`.

Medium and fine each produced:

- `30` sampled section rows, `5` per required span.
- `segment_friction.csv` with `13` lines.
- `momentum_budget.csv` with `7` lines.
- source decomposed `T` diagnostics with zero nonfinite numeric tokens.

Slurm job `3288637` was submitted from `login3` at
`2026-07-10T12:06:54-0500` and confirmed as:

```text
3288637|PENDING|2026-07-10T20:00:00|2026-07-10T12:06:54|0:00|1|(BeginTime)|agent248_salt2_pressure
```

After the user asked to run the short path in the background on this compute
node, the same pressure-only rerun was launched in `tmux` session
`agent248_pressure_now` on `c318-008.ls6.tacc.utexas.edu`.

Immediate rerun:

- Log:
  `work_products/2026-07/2026-07-10/2026-07-10_salt2_refined_pressure_smoke_and_8pm_batch/logs/background_20260710T164330-0500_pressure.log`
- Start: `2026-07-10T16:43:22-0500`
- Finish: `2026-07-10T16:48:08-0500`
- Elapsed: `00:04:46`
- Medium/fine both reused reconstructed pressure times.
- Medium/fine both passed the same section/friction/momentum gates.

Because the immediate rerun completed and the 8pm job would have duplicated the
same outputs, job `3288637` was canceled. `sacct` reported `CANCELLED+`,
elapsed `00:00:00`, with `None assigned`.

## Files

- Package:
  `work_products/2026-07/2026-07-10/2026-07-10_salt2_refined_pressure_smoke_and_8pm_batch/`
- Generator:
  `tools/analyze/build_salt2_refined_pressure_smoke_and_8pm_batch.py`
- Test:
  `tools/analyze/test_salt2_refined_pressure_smoke_and_8pm_batch.py`
- Manifest:
  `imports/2026-07-10_salt2_refined_pressure_smoke_and_8pm_batch.json`
- Journal:
  `.agent/journal/2026-07-10/salt2-refined-pressure-smoke-and-8pm-batch.md`

## Follow-On Instructions

No 8pm job remains to monitor. To review the completed immediate rerun, check:

```bash
ssh login3 'sacct -j 3288637 --format=JobID,JobName,State,ExitCode,Elapsed,NodeList%30'
tail -80 work_products/2026-07/2026-07-10/2026-07-10_salt2_refined_pressure_smoke_and_8pm_batch/logs/background_20260710T164330-0500_pressure.log
```

The next useful step is pressure-only mesh-family comparison and sign/fit-target
review, not another duplicate extraction. Do not admit thermal closure rows from
this package.

## Caveats

This task only proves pressure/friction/momentum extraction readiness. Several
derived friction rows carry `negative_f_pressure_recovery_or_noise`; those need
sign-convention and station-retention review before closure fitting or GCI
admission. Thermal UA/HTC/Nu remains blocked by the AGENT-245 reconstructed-`T`
parse failure.
