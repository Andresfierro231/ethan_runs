# AGENT-194 Status

Date: `2026-07-07`
Role: Implementer
Owner: claude

## Scope

Build a per-segment patchwise heat source/sink ledger for Salt 2/3/4 Jin mainline
continuations.  Captures wallHeatFlux integral per (source_id, patch_group, span)
from postProcessing FO output, joins with mdot and T_bulk from existing HTC work
product, records BC types, and computes a global energy-balance check.

## Completed

- Read 4 required pre-edit files (AGENTS.md, BOARD.md, FILE_OWNERSHIP.md, ROLES.md)
- Confirmed AGENT-194 row is claimed in BOARD.md Active table
- Explored data sources:
  - `postProcessing/wallHeatFlux/*/wallHeatFlux.dat` (tab-delimited, column 5 = Q [W] integrated)
  - `0/T` BC file for BC type extraction (rcExternalTemperature, externalTemperature, zeroGradient)
  - `postProcessing/mdot_pipeleg_lower_05_straight/*/surfaceFieldValue.dat` for mdot
  - `work_products/2026-06-30_claude_thermal_htc/segment_htc_uaprime_*.csv` for T_bulk per span
- Wrote `tools/analyze/build_heat_source_sink_ledger.py`:
  - wallHeatFlux.dat reader (latest time step from highest-numbered time dir)
  - 0/T BC type parser (regex-based, handles quoted patch blocks)
  - mdot reader (surfaceFieldValue.dat, last value, absolute)
  - Patch classification: 37 named patches + junction wildcard → (patch_group, span)
  - Group aggregation: wallHeatFlux_integral_W summed per (patch_group, span)
  - enthalpy_change_W = NaN (only mid-span T_bulk available, not inlet/outlet)
  - Aggregate check: net sum / heater duty per source_id
- Ran script; produced 24 rows (8 per case × 3 cases):
  - Salt 2: heater=243.5 W, net=0.1 W (0.052% of |heater|) — PASS
  - Salt 3: heater=273.2 W, net=0.3 W (0.105% of |heater|) — slight NOTE
  - Salt 4: heater=310.5 W, net=0.1 W (0.026% of |heater|) — PASS
- Wrote `tools/analyze/test_heat_source_sink_ledger.py` (36 tests):
  - TestClassifyPatch (12 tests) — pure unit tests
  - TestParseWallHeatFlux (4 tests) — synthetic dat file tests
  - TestParseBCTypes (4 tests) — synthetic T file tests
  - TestAggregateCheck (4 tests) — synthetic row tests
  - TestLedgerCSV (12 tests) — integration tests against generated CSV
- All 36 new tests PASS; full suite 288 tests PASS
- Wrote work_products/2026-07-07_heat_source_sink_ledger/:
  - heat_source_sink_ledger.csv (24 rows, 17 columns)
  - heat_source_sink_ledger.json
  - README.md (includes resistance network note, sign convention, test section paradox)
  - summary.json
- Wrote `.agent/status/2026-07-07_AGENT-194.md` (this file)
- Wrote `.agent/journal/2026-07-07/implementer-heat-source-sink-ledger.md`

## Current State

COMPLETE. All deliverables written and tests passing.

Key findings:
1. Heater duty (wallHeatFlux at inner wall): 243.5 / 273.2 / 310.5 W for Salt 2/3/4
   (these are the actual flux INTO the fluid, not the BC-specified 265.7/297.5/337.6 W;
   difference is heat lost through heater insulation to ambient)
2. Cooler removal (cooler + reducers): -136.4 / -150.8 / -169.2 W for Salt 2/3/4
3. Test section (pipeleg_left_04): NEGATIVE wallHeatFlux (-5.7/-10.5/-16.8 W) despite
   Q=37 W electrical input — the hot upcomer fluid (456–485 K) loses heat to ambient
   through the quartz despite the heater
4. Net balance within 0.1% of heater duty for Salt 2/4; Salt 3 at 0.105% (quasi-steady)
5. Junction losses: 39–48 W per case (largest single passive loss term)
6. enthalpy_change_W = NaN: HTC CSV provides only mid-span T_bulk, not inlet/outlet

## Follow-up

- To compute enthalpy_change_W: extract T_bulk at NCC interface stations (TW2/TW4/TW6/TW8)
  from continuation postProcessing temperature_probes or wall_temperature_probes
- Resistance network decomposition (wall conduction → external h per layer) is a planned
  future task (requires wall thickness and conductivity per patch, which are in 0/T)
- Salt 3 net balance at 0.105% (just above 0.1% gate): acceptable for quasi-steady CFD;
  could be reduced by averaging over a time window rather than using a single time step
