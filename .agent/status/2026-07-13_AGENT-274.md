# AGENT-274 Status

Date: `2026-07-13`

Role: Coordinator / Implementer / Tester / Writer

## Scope

- Decide whether `salt4_hi10q` needed continued runtime before postprocessing.
- Update the selected corrected Salt-Q continuation launcher for a four-row,
  one-node packed run.
- Submit the packed job only if lightweight validation passes.
- Document the decision, scheduler state, and any unresolved follow-up.

## Current Findings

- Prior allocation `3288671` is terminal: `COMPLETED`, elapsed `3-00:23:10`,
  node `c318-017`.
- Current user-visible Slurm queue before submission only showed interactive job
  `3292998` on `c318-008`.
- Salt4 +10Q should be included in the packed continuation. Its final available
  written-monitor window, `12039` to `12639 s`, was still materially changing:
  - `total_Q`: latest `6.99361 W`, mean `7.71885485857 W`, drift
    `-1.59099 W`, span `1.69738 W`.
  - mass-flow monitors: drift about `-1.13e-4 kg/s` over the final 600 s,
    approximately `0.61%` of mean, with `~2.4%` window spans.
  - fluid temperature probes: max final-window drift `1.45105 K`, max span
    `4.73688 K`.
  - wall temperature probes: max final-window drift `1.52954 K`, max span
    `4.07394 K`.

## Implementation

- `run_selected_corrected_salt_q_continuation.sbatch` now launches:
  - `salt2_lo10q`
  - `salt2_hi10q`
  - `salt4_lo10q`
  - `salt4_hi10q`
- The script keeps `#SBATCH -n 256`, `NTASKS_PER_CASE=64`, and adds an explicit
  `MAX_CONCURRENT_CASES=4` guard plus an allocated-task guard.
- The selected staged `system/functions` files for `salt2_lo10q`,
  `salt2_hi10q`, and `salt4_lo10q` now match `salt4_hi10q`: the convergence
  monitor is diagnostic-only and no longer calls `stopAt(writeNow)`.
- Submitted packed Slurm job `3293441` from `login3`.

## Validation

- `bash -n jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/scripts/run_selected_corrected_salt_q_continuation.sbatch`: pass.
- `python3.11 -m json.tool imports/2026-07-13_salt_q_four_row_packed_continuation.json`: pass.
- Restart roots present with latest times:
  - `salt2_lo10q`: `10291`
  - `salt2_hi10q`: `9674`
  - `salt4_lo10q`: `11655`
  - `salt4_hi10q`: `12639`
- `rg` scan for `stopAtControl::writeNow` / `stopAt(writeNow)` in selected
  `controlDict` and `system/functions`: no matches after the staged monitor
  patch.
- `squeue -j 3293441`: `RUNNING`, node `c318-011`, time limit `5-00:00:00`.
- `squeue -s -j 3293441`: four active `foamRun` steps, `3293441.0` through
  `3293441.3`, plus the batch step.
- Slurm stdout showed all four restart fields patched and all four cases
  launched at `2026-07-13T13:05:23-05:00` to `13:05:24-05:00`.

## Follow-Up

- Monitor `3293441` and per-case `logs/log.foamRun_corrected_q` for runtime
  advancement beyond restart.
- Re-run corrected-Q convergence/admission only after late windows exist.
