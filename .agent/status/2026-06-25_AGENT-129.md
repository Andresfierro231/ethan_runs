# AGENT-129 Status

- Role: `Coordinator / Implementer / Writer`
- Status: complete
- Scope:
  - `.agent/BOARD.md`
  - `.agent/status/2026-06-25_AGENT-129.md`
  - `.agent/journal/2026-06-25/coordinator-implementer-writer-ethan-runs-backup.md`
  - `imports/2026-06-25_ethan_runs_backup_inventory.json`
  - `operational_notes/06-26/25/2026-06-25_ethan_runs_backup_plan.md`
  - `tools/publish/rsync_ethan_runs_backup.sh`
  - `tools/publish/submit_ethan_runs_backup_sbatch.sh`
- Outcome:
  - Claimed a dedicated backup task and recorded a non-overlapping scope that
    avoids the actively owned `reports/` and `journals/2026-06-25` paths.
  - Added a reusable `rsync` helper that creates a readable backup directory
    layout, writes a top-level backup README, records per-run manifests, and
    separates dry-run delta discovery from the real copy.
  - Added a Slurm submission wrapper for `NuclearEnergy` so the large backup
    sync can run as a compute-node job instead of a login-node transfer.
  - Hardened the backup scripts on `2026-06-29` for live-tree syncs:
    - `rsync_ethan_runs_backup.sh` now supports `--top-level-pass`
    - sync/plan now write `rsync_status.txt`
    - rsync exit code `24` is treated as a non-fatal vanished-file warning
      rather than a hard failure
  - Added scheduled-start support to the submission wrapper via `--begin`.
  - Created the real external backup skeleton under
    `/home1/09748/andresfierro231/bubble_flow_loop/tamu_loop_box/cfd_runs/ethan_runs_backup`
    with `README.md`, `manifests/`, and `mirror/ethan_runs/`.
  - Recorded the current source footprint as about `786,074,703,912` bytes
    (`732.1 GiB`) in the default scope, with dominant trees `jadyn_runs`
    (`497G`), `tmp_extract` (`163G`), and `staging` (`72G`), plus an excluded
    default `tmp/` tree of about `166G`.
  - Documented a conservative walltime recommendation of `10:00:00` for the
    initial default-scope sync, `02:00:00` for routine incremental refreshes,
    `04:00:00` for checksum audits, and `12:00:00` when `tmp/` is included.
  - Submitted the first real backup job from `login3` because `sbatch` is not
    available from the current compute-node shell. Slurm job `3259225`
    (`ethan_backup_sync`) is queued on `NuclearEnergy` with a `10:00:00`
    walltime and the emitted script at
    `tmp/slurm_ethan_runs_backup_jobs/20260625T125336_sync/ethan_backup_sync.sbatch`.
- Validation:
  - `bash -n tools/publish/rsync_ethan_runs_backup.sh`
  - `bash -n tools/publish/submit_ethan_runs_backup_sbatch.sh`
  - `bash tools/publish/rsync_ethan_runs_backup.sh prepare --source-root /tmp/ethan_runs_backup_smoke_src --backup-root /tmp/ethan_runs_backup_smoke_dest`
  - `bash tools/publish/rsync_ethan_runs_backup.sh plan --source-root /tmp/ethan_runs_backup_smoke_src --backup-root /tmp/ethan_runs_backup_smoke_dest --manifest-stamp smoke_plan`
  - `bash tools/publish/rsync_ethan_runs_backup.sh sync --source-root /tmp/ethan_runs_backup_smoke_src --backup-root /tmp/ethan_runs_backup_smoke_dest --manifest-stamp smoke_sync`
  - `bash tools/publish/submit_ethan_runs_backup_sbatch.sh --mode plan --backup-root /tmp/ethan_runs_backup_smoke_dest --job-dir /tmp/ethan_runs_backup_job_smoke --dry-run`
  - `ssh login3.ls6.tacc.utexas.edu 'cd /scratch/09748/andresfierro231/projects_scratch/ethan_runs && bash tools/publish/submit_ethan_runs_backup_sbatch.sh --mode sync --time 10:00:00'`
  - `ssh login3.ls6.tacc.utexas.edu 'squeue -j 3259225 -o "%.18i %.9P %.24j %.8T %.10M %.9l %.20S %.20R"'`
  - `bash -n tools/publish/rsync_ethan_runs_backup.sh`
  - `bash -n tools/publish/submit_ethan_runs_backup_sbatch.sh`
  - `bash tools/publish/rsync_ethan_runs_backup.sh sync --source-root /tmp/ethan_runs_backup_smoke_src --backup-root /tmp/ethan_runs_backup_smoke_dest --mirror-name smoke --manifest-stamp smoke_top_level_sync --top-level-pass`
  - `bash tools/publish/submit_ethan_runs_backup_sbatch.sh --mode sync --time 11:30:00 --begin 2026-06-29T20:00:00 --top-level-pass --job-dir /tmp/ethan_runs_backup_job_schedule_smoke --dry-run`
  - `ssh login3.ls6.tacc.utexas.edu 'cd /scratch/09748/andresfierro231/projects_scratch/ethan_runs && bash tools/publish/submit_ethan_runs_backup_sbatch.sh --mode sync --time 11:30:00 --begin 2026-06-29T20:00:00 --top-level-pass'`
  - `ssh login3.ls6.tacc.utexas.edu 'squeue -j 3265388 -o "%.18i %.24j %.8T %.10M %.9l %.20S %.20R"'`
  - Follow-up:
  - `2026-06-29` follow-up audit:
    - the backup mirror under
      `/home1/09748/andresfierro231/bubble_flow_loop/tamu_loop_box/cfd_runs/ethan_runs_backup/mirror/ethan_runs`
      is no longer empty, so the sync lane has made real progress
    - history manifests exist for sync attempts `20260625T150303` and
      `20260626T200016`, with `rsync_sync.log` files of `874123` and `709036`
      lines respectively
    - `manifests/latest/` still lacks `summary.txt`, `new_files.txt`,
      `changed_files.txt`, and `rsync_plan.log`; only `backup_scope.txt` is
      present there
    - Slurm job `3259614` hit the walltime limit
    - Slurm job `3261333` ended with rsync warning code `24` because live case
      files vanished during transfer
  - Next step:
    - queued overnight follow-up job `3265388`:
      - start: `2026-06-29T20:00:00` CDT
      - walltime: `11:30:00`
      - expected end window: `2026-06-30T07:30:00` CDT
      - script:
        `tmp/slurm_ethan_runs_backup_jobs/20260629T133052_sync/ethan_backup_sync.sbatch`
      - mode: `sync --top-level-pass`
    - `2026-06-30` final inspection by `AGENT-159`:
      - `manifests/latest/summary.txt` exists for stamp `20260629T200027`
      - mode is `sync`, `top_level_pass = 1`, `include_tmp = 0`
      - latest counts are `184439` new files, `694` changed files,
        `58611` created directories, and `66` symlink updates
      - `manifests/latest/rsync_status.txt` marks every tracked root `ok`
        except `jadyn_runs`, which is `ok_with_vanished_files` with rsync
        code `24`
      - the code `24` warning is accepted as live-tree churn for this backup
        lane because the remaining top-level roots completed normally
    - future backup refreshes should be opened as new routine operations tasks
      rather than keeping `AGENT-129` active
