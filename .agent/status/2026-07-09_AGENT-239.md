# AGENT-239 Status

Updated: `2026-07-09T17:18:00-05:00`

Role: Coordinator / Implementer / Tester / Writer

## Scope

- Added `tools/analyze/build_salt2_fine_closure_qoi_smoke.py`.
- Added `tools/analyze/test_salt2_fine_closure_qoi_smoke.py`.
- Generated `work_products/2026-07/2026-07-09/2026-07-09_salt2_fine_closure_qoi_smoke_and_overnight/`.
- Added import manifest `imports/2026-07-09_salt2_fine_closure_qoi_smoke_and_overnight.json`.

## Execution Result

- `srun` preflight passed on compute node `c318-008`:
  - OF13 ready.
  - `python3` has `numpy 1.21.2` and `scipy 1.6.1`.
  - Salt2 medium latest time `518` has required fields in `processors64`.
  - Salt2 fine latest time `399` has required fields in `processors128`.
- Persistent run `agent239_salt2_refined_r2` completed the full medium+fine chain:
  - medium completed at `2026-07-09T16:44:14-05:00`.
  - fine completed at `2026-07-09T16:58:31-05:00`.
- Log: `work_products/2026-07/2026-07-09/2026-07-09_salt2_fine_closure_qoi_smoke_and_overnight/logs/tmux_smoke_then_overnight_r2.log`.
- Outputs exist under `outputs/medium/` and `outputs/fine/`.

## Cancelled Batch Rerun

- A 10pm `sbatch` wrapper was created at `scripts/sbatch_smoke_then_overnight_10pm.sh`.
- Job `3287480` was submitted for `2026-07-09T22:00:00`.
- The job was cancelled after user clarification because it would only rerun the
  same completed `smoke_then_overnight` extraction and would not add a new
  analysis stage.

## Smoke Finding Already Fixed

The first persistent run failed before field reconstruction because the staged
`controlDict` included `system/functions` after the runner removed that file.
The generator now writes an empty include-compatible `system/functions` file in
the staged reconstruction mirror instead of deleting it.

## Validation

- `python3.11 -m py_compile tools/analyze/build_salt2_fine_closure_qoi_smoke.py tools/analyze/test_salt2_fine_closure_qoi_smoke.py` passed.
- Manual test assertions passed.
- `pytest` remains unavailable for `python3.11`.
- `srun` preflight passed after Slurm socket access was approved.

## Boundaries

- No source solver-output mutation.
- No registry edit.
- No closure-observation update.
- No Salt4 fine processing; Salt4 medium/fine remain blocked by admission status.
