# AGENT-245 Status

Updated: `2026-07-10T00:00:00-05:00`

Role: Coordinator / Implementer / Tester / Writer

## Objective

Submit a 9pm Slurm batch job to rerun Salt2 medium/fine refined-mesh
closure-QOI extraction with hard gates, because AGENT-239 produced files but
section pressure sampling failed (`no_output` station rows and empty
`segment_friction.csv`).

## Submitted Job

- Job ID: `3287580`
- Job name: `agent245_salt2_qoi`
- Scheduled start: `2026-07-09T21:00:00`
- Final state: `FAILED`, exit code `1:0`
- Runtime: `00:03:05`
- Node: `c318-018`
- Batch script:
  `work_products/2026-07/2026-07-09/2026-07-09_salt2_refined_closure_qoi_repair_batch/scripts/sbatch_repair_extraction_9pm.sh`

## Result

The job did **not** produce closure-ready medium/fine outputs. It failed in the
medium section-sampling stage before fine was attempted.

Observed failure:

- `reconstructPar` completed under OpenFOAM 13.
- `foamPostProcess` also ran under OpenFOAM 13.
- `foamPostProcess` failed while reading reconstructed medium `518/T`:
  `wrong token type - expected Scalar ... punctuation token '-'`.
- The reconstructed `recon/medium/518/T` contains garbage values and `-nan`
  around the fatal line; the source decomposed
  `processors64/518/T` has normal temperatures in the same inspected range.
- The hard gate correctly stopped the batch before empty friction/GCI products
  could be generated.

Current outputs are not closure evidence:

- `outputs/medium/section_mean_pressure_*.json` still has 30/30
  `status=no_output` rows.
- `outputs/medium/segment_friction.csv` was not produced.
- `outputs/fine/**` was not attempted in this repair batch.

## Package

`work_products/2026-07/2026-07-09/2026-07-09_salt2_refined_closure_qoi_repair_batch/`

The runner stages fresh local mirrors, keeps source case trees read-only, avoids
symlinking old monitor `postProcessing` folders before section sampling, and
hard-fails if pressure/thermal cut-plane outputs are missing.

## Fresh-Agent Continuation

1. Do not use AGENT-239 or AGENT-245 section/friction outputs as closure
   evidence.
2. Repair path A: rerun pressure-only section sampling without
   `--dump-temperature` so pressure/friction can proceed using `U p_rgh rho`.
3. Repair path B: diagnose why reconstructed medium `T` is corrupted before
   retrying thermal cut-plane sampling; compare reconstructed serial `T` against
   decomposed processor `T`.
4. After pressure-only extraction works, run medium/fine friction and momentum
   reductions.
5. Thermal UA/HTC/Nu remains blocked until the T reconstruction/read path is
   repaired or replaced.

## Validation

- Python compile passed.
- Manual generator assertions passed.
- Generated runner and sbatch scripts passed `bash -n`.
- `pytest` remains unavailable under `python3.11`.

## Boundaries

- No source solver-output mutation.
- No closure-observation update.
- No registry edit.
- Salt4 medium/fine remain blocked by admission/continuation status.
