---
provenance:
  - work_products/2026-07/2026-07-17/2026-07-17_post_terminal_cfd_harvest_watch/README.md
  - work_products/2026-07/2026-07-17/2026-07-17_post_terminal_cfd_harvest_watch/live_scheduler_snapshot.csv
  - work_products/2026-07/2026-07-17/2026-07-17_post_terminal_cfd_harvest_watch/terminal_admission_trigger_matrix.csv
tags: [status, scheduler, cfd-pp, harvest-watch, admission]
related:
  - .agent/journal/2026-07-17/post-terminal-cfd-harvest-watch.md
  - imports/2026-07-17_post_terminal_cfd_harvest_watch.json
task: AGENT-519
date: 2026-07-17
role: Coordinator/Scheduler/cfd-pp/Writer
type: status
status: active
---
# AGENT-519 Status

## Objective

Assign a read-only scheduler monitor for live and future CFD/postprocessing
jobs that may unlock terminal-admission packages. This row intentionally does
not submit, cancel, requeue, mutate dependencies, run solvers, run harvest
postprocessing, or change scientific admission state.

## Changes Made

- Claimed active board row `AGENT-519`.
- Created watch package:
  `work_products/2026-07/2026-07-17/2026-07-17_post_terminal_cfd_harvest_watch/`
- Recorded a current scheduler/accounting snapshot for watched jobs.
- Recorded trigger rules that decide when a separate harvest/admission row
  should be claimed.
- Wrote explicit duplicate-submission guardrails.

## Current Snapshot

Read-only `squeue` and `sacct` checks on 2026-07-17 show:

- `3293924` / `saltq_sel_cont`: `RUNNING`, elapsed `3-22:51:08`; batch plus
  four `foamRun` steps are running.
- `3295438` / `saltq_s24_sel_harv`: `PENDING`, dependency-held; do not submit
  a duplicate selected Salt2/Salt4 corrected-Q harvester.
- `3299610` / `salt4_q3x_probe`: `RUNNING`, elapsed `22:15:43`.
- `3299620` / `salt4_heat_pack`: `RUNNING`, elapsed `22:00:54`.
- `3300966` / `ag511_hsrc`: `RUNNING`; this is active AGENT-511 scope and is
  logged as context only, not a monitor trigger owned by AGENT-519.

## 2026-07-21 Refresh

Read-only `squeue` and `sacct` checks at `2026-07-21T17:19:14-0500` show:

- `3299610` / `salt4_q3x_probe`: `RUNNING`, elapsed `4-23:39:53`, time limit
  `5-00:00:00`, node `c318-017`.
- `3299620` / `salt4_heat_pack`: `RUNNING`, elapsed `4-23:25:04`, time limit
  `5-00:00:00`, node `c318-018`.
- `3307441` / `saltq_sel36_cont`: `RUNNING`, elapsed `08:21:05`, time limit
  `1-12:00:00`, node `c318-020`; four `foamRun` steps are running.

No watched job is terminal-success at this snapshot, so no harvest or
admission row is triggered. If `3299610` or `3299620` complete successfully,
claim a separate S10/F6 pressure-anchor harvest/preflight row. If `3307441`
completes successfully, claim a separate S9/S13 corrected-Q source-family
harvest/preflight row.

## 2026-07-21 18:25 Refresh

Read-only `squeue` and `sacct` checks at `2026-07-21T18:25:14-0500` show:

- `3299610` / `salt4_q3x_probe`: `TIMEOUT`, elapsed `5-00:00:25`; batch and
  `foamRun` step cancelled by timeout. This is not terminal success and does
  not trigger harvest/admission.
- `3299620` / `salt4_heat_pack`: `TIMEOUT`, elapsed `5-00:00:08`; batch and
  three `foamRun` steps cancelled by timeout. This is not terminal success and
  does not trigger harvest/admission.
- `3307441` / `saltq_sel36_cont`: `RUNNING`, elapsed `09:26:50`, time limit
  `1-12:00:00`, node `c318-020`; four `foamRun` steps are still running.

Current action: keep `AGENT-519` active for corrected-Q monitoring only. The
timed-out high-heat jobs require a separate timeout-disposition or recovery row
if more work is desired; do not harvest, resubmit, cancel, requeue, or admit
from this monitor row.

## 2026-07-21 18:44 Refresh

Read-only `squeue` and `sacct` checks at `2026-07-21T18:44:45-0500` show:

- `3299610` / `salt4_q3x_probe`: `TIMEOUT`, unchanged; no
  harvest/admission trigger.
- `3299620` / `salt4_heat_pack`: `TIMEOUT`, unchanged; no
  harvest/admission trigger.
- `3307441` / `saltq_sel36_cont`: `RUNNING`, elapsed `09:46:18`, time limit
  `1-12:00:00`, node `c318-020`; four `foamRun` steps are still running.
- `3308712` / `salt4_heat2_pack`: `RUNNING`, elapsed `00:39:15`, time limit
  `5-00:00:00`, node `c318-017`; four `foamRun` steps are running.

Current action: monitor `3307441` and `3308712` only. Neither is terminal
success, so no harvest, sampler, postprocessing, registry/admission, or score
row is triggered. If `3308712` lands successfully, claim a separate
high-heat steady-state disposition/harvest-readiness row before any sampling or
F6 use.

## 2026-07-22 11:03 Refresh

Read-only `squeue` and `sacct` checks at `2026-07-22T11:03:40-0500` show:

- `3307441` / `saltq_sel36_cont`: `RUNNING`, elapsed `1-02:05:31`,
  node `c318-020`; four `foamRun` steps remain running.
- `3308712` / `salt4_heat2_pack`: `RUNNING`, elapsed `16:58:28`, node
  `c318-017`; four `foamRun` steps remain running.
- `3310179` / `s13_mf_sampler`: `COMPLETED`, exit code `0:0`, elapsed
  `00:52:15`, node `c318-019`.

The S13 sampler's scheduler completion is not a harvest/admission trigger:
the task package reports decision `partial_or_failed_sampling_fail_closed`,
`exact_label_qoi_rows=0`, and `sampling_error_rows=6`. The next S13 action is a
separate face-area-vector repair row, not AGENT-519 harvest.

Current action: keep `AGENT-519` active for `3307441` and `3308712`. No
terminal-success harvest trigger exists for corrected-Q or high-heat evidence.

## 2026-07-22 12:52 Refresh

Read-only `sacct` checks at `2026-07-22T12:52:05-0500` show:

- `3307441` / `saltq_sel36_cont`: `RUNNING`, elapsed `1-03:53:43`,
  node `c318-020`; batch plus four `foamRun` steps remain running.
- `3308712` / `salt4_heat2_pack`: `RUNNING`, elapsed `18:46:40`,
  node `c318-017`; batch plus four `foamRun` steps remain running.

No watched job is terminal-success at this snapshot. No harvest, parser,
postprocessing, registry/admission, score, cancellation, requeue, or duplicate
submission is triggered.

## Validation

- `squeue -j 3299610,3299620,3307441 -o '%i|%j|%T|%M|%l|%D|%R'` passed.
- `sacct -j 3299610,3299620,3307441 --format JobID,JobName,Partition,Account,State,Elapsed,ExitCode -P` passed.
- `squeue -j 3308712,3307441` passed.
- `sacct -j 3308712,3307441,3299610,3299620 --format=JobID,JobName%34,State,ExitCode,Elapsed,Timelimit,NodeList%30` passed.
- `squeue -j 3307441,3308712,3310179 -o '%i|%j|%T|%M|%l|%D|%R'` passed after
  a malformed unquoted format attempt was stopped; `3307441` and `3308712`
  were running, while `3310179` was absent from live queue because completed.
- `sacct -j 3307441,3308712,3310179 --format=JobID,JobName,State,ExitCode,Elapsed,Start,End,NodeList` passed.
- `sacct -j 3307441,3308712 --format=JobID,JobName,State,ExitCode,Elapsed,Start,End,NodeList -P` passed at the 2026-07-22 12:52 refresh.
- `git diff --check` on AGENT-519 touched files passed.
- `python3.11 tools/agent/finish_task.py --task-id AGENT-519` was intentionally
  not a closeout pass because the board row remains active; it reported the
  expected active-status error before this validation section was added.

## Trigger Policy

When a watched job is terminal and successful, AGENT-519 should stop at a
handoff: claim or create a separate cfd-pp/admission row before any harvest,
parsing, registry, scorecard, or admission work begins. Failed, cancelled, or
timeout states should be documented with log paths and should not trigger
automatic resubmission.

## Guardrails

No native CFD/OpenFOAM outputs, registry/admission state, scheduler state,
generated index files, Fluid source, or active agent scopes were mutated. No
solver, postprocessing, harvest, submission, cancellation, requeue, dependency
edit, fit, tuning, model selection, or admission change was run.

## Next Check

Run read-only queue/accounting checks for `3307441,3308712`.
Terminal-success for any watched job should be handed to a separate claimed
harvest/preflight row before parsing, harvest, score, registry, or admission
work.
