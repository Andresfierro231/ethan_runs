# AGENT-130 Status

- Role: `Coordinator / Implementer / Writer`
- Status: completed
- Date: `2026-06-25`

## Scope

- `.agent/BOARD.md`
- `.agent/status/2026-06-25_AGENT-130.md`
- `.agent/journal/2026-06-25/coordinator-implementer-writer-postprocessing-registry-pipeline.md`
- `imports/2026-06-25_ethan_postprocessing_registry_pipeline.json`
- `tools/extract/postprocessing_registry_common.py`
- `tools/extract/aggregate_registered_postprocessing.py`
- `tools/extract/test_postprocessing_registry.py`
- `tools/analyze/plot_temperature_probes.py`
- `tools/analyze/plot_wall_temperature_probes.py`
- `tools/analyze/plot_velocity_profiles.py`
- `tools/analyze/test_postprocessing_plots.py`
- real smoke outputs under `registry/salt2/ethan_modern_runs_staged/salt_test_2_kirst/viscosity_screening_salt_test_2_kirst_coarse_mesh/**`
- global/family index smoke outputs under `registry/_all_postprocessing_runs.{csv,sqlite}` and `registry/salt2/_family_index.{csv,sqlite}`

## Validation

- `python3.11 -m unittest tools.extract.test_postprocessing_registry tools.analyze.test_postprocessing_plots`
- `python3.11 -m py_compile tools/extract/postprocessing_registry_common.py tools/extract/aggregate_registered_postprocessing.py tools/analyze/plot_temperature_probes.py tools/analyze/plot_wall_temperature_probes.py tools/analyze/plot_velocity_profiles.py tools/extract/test_postprocessing_registry.py tools/analyze/test_postprocessing_plots.py`
- `python3.11 tools/extract/aggregate_registered_postprocessing.py --source-id viscosity_screening_salt_test_2_kirst_coarse_mesh`
- `python3 tools/analyze/plot_temperature_probes.py --source-id viscosity_screening_salt_test_2_kirst_coarse_mesh --output-name real_tp_smoke --include TP1 TP4`
- `python3 tools/analyze/plot_velocity_profiles.py --source-id viscosity_screening_salt_test_2_kirst_coarse_mesh --output-name real_velocity_smoke --times 586.56 --component Uy --profile-axis both`

## Notes

- Fast-format output landed as SQLite instead of Parquet because the current environment lacks `pyarrow` and `pandas`.
- `python3` has `matplotlib`; `python3.11` does not. Plot smoke validation therefore used `python3`, while parser/aggregation tests used `python3.11`.
- The normalized long table was revised to exclude `velocity_profile` rows. Velocity-profile plotting now reads raw `postProcessing/velocity_profiles` data by default, which preserves the plotting workflow without inflating `postprocessing_case_long.csv` or the main SQLite table.
- The second requested aggregation remains implemented as `wall_heat_flux_grouped.csv`, with `total_Q_postProc` kept as the first column.
- A detached full-sweep registry aggregation was launched on the compute node in tmux session `agent130_postproc` at `2026-06-25 14:22 CDT`, with progress logged to `tmp/postprocessing_runs/aggregate_registered_postprocessing_2026-06-25_v2.log`.
- The full foreground rerun completed successfully at `2026-06-25 16:44 CDT` for all `13` registered source ids, updating all per-run aggregates plus family/global indexes under `registry/`.
- Quick validation after the sweep found `13/13` index rows with all expected output files present, correct `wall_heat_flux_grouped.csv` header ordering, and exactly one `mdot` discrepancy case: `val_water_test_4_coarse_mesh_laminar`.
- Real-output TP/TW plotting sweeps also completed for all `13` cases, producing default `svg` and `png` figures under each case-local `plots/temperature_probes/` and `plots/wall_temperature_probes/` directory.
- The velocity-profile plot CLI was optimized so default raw-data plotting reads only the selected profile times instead of the full history for every run. A real-output final-timestep sweep then completed for all `13` cases, producing `registry_velocity_final.{svg,png}` under each case-local `plots/velocity_profiles/` directory.
