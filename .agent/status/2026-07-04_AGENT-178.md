# AGENT-178 Status

Date: `2026-07-04`
Role: Coordinator / Implementer / Writer
Owner: codex

## Scope

Quarantine invalid Salt perturbation data, document the BC/restart-field root
cause, stage corrected Salt Q perturbation cases, run local smoke checks, and
submit the corrected production wave.

## Completed

- Documented the invalid historical Salt perturbation roots and strengthened the
  T3 quarantine note.
- Created corrected campaign
  `jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/`.
- Added scripts to patch root `0/T` and copied decomposed restart `T` fields.
- Staged 14 corrected cases from nominal Jin continuations.
- Passed local syntax, Python compile, root patch, restart-field smoke, and
  OpenFOAM environment checks.
- Found and fixed three launcher issues in failed/canceled attempts: strict-mode
  OpenFOAM sourcing, Slurm comma export splitting, and broken post-OpenFOAM
  `python` resolution with unenforced patcher failure.
- Found and fixed a fourth restart-patching issue: collated
  `decomposedBlockData` `T` files require binary-frame-preserving edits with
  recomputed processor block byte counts.
- Found and fixed a fifth launch issue: corrected staged roots need parent
  `dynamicCode/` to avoid fresh parallel coded-function compilation failures.
- Deleted and regenerated the staged `runs/` tree after the bad launch attempts.
  Corrected jobs `3275448`-`3275451` launched.
- Reduced the live Salt perturbation footprint at user request: canceled
  `3275451`, canceled Salt3 high-Q steps `3275450.0` and `3275450.1`, left
  Salt4 low-Q steps `3275450.2` and `3275450.3` running, and submitted delayed
  Salt4 high-Q replacement job `3275558` from `login3` with dependency
  `afterany:3275450`.
- Corrected that operational change at user request: canceled `3275450` and
  canceled pending `3275558`; added `corr_saltq_salt4_all` to `job_groups.tsv`;
  submitted replacement `sbatch` job `3275560` from `login3` for the four
  Salt4 cases only. `3275560` passed launch preflight for all four cases and
  has four running 64-rank `foamRun` steps.
- Added closeout documentation explaining `insulated.h`, patchwise thermal
  resistance, restart-vs-fresh rationale, and the permanent preflight gate.

## Current State

Corrected rows remain inadmissible until a post-run operating-point gate
confirms mdot moved and re-plateaued. Current running jobs are `3275448`,
`3275449`, and Salt4-only replacement `3275560`. Jobs `3275450`, `3275451`, and
`3275558` are canceled. The submission state is recorded in
`submitted_jobs.csv` under the corrected campaign directory.

## Follow-up

Inspect `preflight_patch_audit_<job>.csv` and `logs/log.foamRun_corrected_q`
after launch. Re-run the perturbation requalification gate after late windows
exist, and admit only `requalified` rows to closure or 1D model fits. Monday
also needs a reusable automated preflight checker that fails a case unless
`case_config.yaml`, root `0/T`, and `processors64/<latest>/T` agree for every
target thermal patch, including collated restart-frame validation and a
documented audit artifact.

## 2026-07-07 Update: Salt 1 Convergence Monitor Early Stop

### Objective

Answer whether Salt 1 stopped because it reached target `endTime` or because a
convergence monitor terminated the run, then prevent the same mistake in future
Salt 1 corrected-Q continuations.

### Inputs / Provenance Inspected

- `salt1_jin_hi10q_corrected` `logs/log.foamRun_corrected_q`
- Salt 1 corrected low/high Q `system/controlDict`
- Salt 1 corrected low/high Q `system/functions`
- `jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/README.md`
- `jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/TODO.md`
- `operational_notes/06-26/07/2026-06-07_runtime_operator_reference.md`

### Planned Outputs

- Salt 1 corrected low/high Q convergence monitor made diagnostic-only.
- OpenFOAM runtime lesson documented outside the campaign-specific note.
- Campaign README/TODO/journal updated with the run-control lesson.

### Acceptance Criteria

- No Salt 1 corrected-Q `system/functions` file retains
  `stopAt(Time::stopAtControl::writeNow)` in `convergenceMonitor`.
- Documentation states that `controlDict` targeted `endTime 9756.33125`, while
  high-Q stopped at `4010.590361446 s` via `convergenceMonitor`.
- Salt 1 remains non-admissible until operating-point movement and
  quasi-steady time-window UQ gates pass.

### Commands Run

```bash
rg -n "AGENT-181|salt1|corrected_salt|controlDict|functions|lessons|OpenFOAM|openfoam" ...
sed -n '1,220p' .agent/BOARD.md
sed -n '1,220p' .agent/FILE_OWNERSHIP.md
sed -n '1,220p' jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/scripts/run_packed_corrected_salt_q.sbatch
sed -n '1,220p' jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/README.md
sed -n '1,220p' .agent/status/2026-07-04_AGENT-178.md
rg -n "convergenceMonitor|stopAt\\(|writeNow|rtol|endTime|stopAt" jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/runs/salt1_jin_*_corrected/case_stage/*/system/functions jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/runs/salt1_jin_*_corrected/case_stage/*/system/controlDict
sed -n '503,660p' jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/runs/salt1_jin_hi10q_corrected/case_stage/viscosity_screening_salt_test_1_jin_coarse_mesh_continuation/system/functions
rg -n "runTimeModifiable|writeControl|writeInterval|purgeWrite|functions" jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/runs/salt1_jin_*_corrected/case_stage/*/system/controlDict
sed -n '1,120p' operational_notes/06-26/07/2026-06-07_runtime_operator_reference.md
```

### Files Changed / Generated

- `jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/runs/salt1_jin_hi10q_corrected/case_stage/viscosity_screening_salt_test_1_jin_coarse_mesh_continuation/system/functions`
- `jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/runs/salt1_jin_lo10q_corrected/case_stage/viscosity_screening_salt_test_1_jin_coarse_mesh_continuation/system/functions`
- `jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/README.md`
- `jadyn_runs/modern_runs/2026-07-04_corrected_salt_q_perturbations/TODO.md`
- `operational_notes/06-26/07/2026-06-07_runtime_operator_reference.md`
- `.agent/journal/2026-07-04/salt-perturbation-quarantine-and-corrected-relaunch.md`
- `.agent/status/2026-07-04_AGENT-178.md`
- `.agent/BOARD.md`

### Raw Observations

- The packed launcher uses `#SBATCH -p NuclearEnergy` and `#SBATCH -A ASC23046`.
- Salt 1 high-Q `controlDict` had `stopAt endTime` and
  `endTime 9756.33125`.
- Salt 1 high-Q log reported `convergenceMonitor: CONVERGED at iteration
  640600` at `Time = 4010.590361446s`, followed by `End`.
- Salt 1 corrected low/high Q `system/functions` both contained a
  `convergenceMonitor` branch that called
  `Time::stopAtControl::writeNow`.
- Verification after the edit found no remaining `stopAt(` or `writeNow`
  matches in the two Salt 1 corrected `system/functions` files.
- `salt1_jin_lo10q_corrected` was still advancing at
  `Time = 5601.396103896s` in the inspected log tail. No log evidence was found
  that the already-running process had reloaded the changed function object.

### Interpretation

- The failed dependency-gated Slurm submission was blocked by allocation
  accounting, not by use of the wrong partition. It targeted `NuclearEnergy`,
  but `ASC23046` had more SUs already requested than remaining balance.
- Salt 1 high-Q stopped because the coded `convergenceMonitor` changed run
  control to `writeNow`, not because the configured target `endTime` was
  reached.
- Global volume-mean flatness is insufficient for corrected perturbation
  admission because it does not prove post-restart operating-point movement,
  relaxation completion, or quasi-steady uncertainty.
- The diagnostic-only function edit is certain to apply to future Salt 1
  continuations/reruns. It may or may not alter the already-loaded live low-Q
  process, so do not rely on the current process as proof that the fix was
  dynamically reloaded.

### Blockers

- Existing Salt 1 high-Q output remains short and non-admissible.
- New continuation submissions remain blocked until allocation capacity frees
  up or an approved alternate allocation is used.

### Exact Next Action

When queue/accounting permits, submit Salt 1 corrected-Q continuation/rerun
from the latest valid restart using the now diagnostic-only monitor, then admit
only if the operating-point gate and quasi-steady time-window UQ pass. Keep
Salt 1 corrected-Q rows excluded from closure fitting until then.
