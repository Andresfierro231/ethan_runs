# AGENT-156 Status

- Task: Address the 8 ranked action items from the 2026-06-30 claude-inspection
  review of the CFD post-processing → 1D closure pipeline, via new reusable
  scripts + heavy documentation, with no collision against active codex tasks.
- Role: Coordinator / Implementer / Writer
- Owner: claude
- Started: `2026-06-30T11:00:00-05:00`
- Scope reference: see the AGENT-156 row on `.agent/BOARD.md`.

## Guiding principles (user-stated)

1. Carefully disclose confidence boundaries, assumptions, and limitations.
2. High scientific rigor: every decision must carry an explicit recorded "why".
3. Heavy documentation + reusable workflows so a paper can be compiled and so
   work is easy to resume after a break.

## Scope decisions (user, 2026-06-30)

- Case scope: **Salt Jin only (paper-grade)** — Salt 2/3/4 Jin primary, Salt 1
  Jin caveated; water and Kirst are sensitivity context only.
- Execution: run on this LS6 compute node where possible; only suggest sbatch
  for runs that would exceed ~1 hour.

## Environment facts established

- On LS6 compute node `c318-008` (interactive, 1 core allocated).
- OpenFOAM reachable via `module load intel/24.1 impi/21.12 openfoam/12`
  (Foundation v12; install `/home1/apps/intel24/impi21/OpenFOAM/OpenFOAM-12`).
- Cases are Foundation **v13** collated (`decomposedBlockData`, binary,
  `processors64/`). v12 tools read them for standard-BC fields.
- Custom BC `rcExternalTemperature` (lib `libRCWallBC.so`, not available for
  v12) is present ONLY on `T`. Fields `p, p_rgh, U, phi, rho` (and derived
  `Re, Pr, Nu, Gr, Ra, Ri`) use standard BCs and ARE reconstructable now.
  Density for buoyancy/HTC work can use the stored `rho` field, avoiding `T`.

## Action-item tracker

| # | Item | Approach | State |
| --- | --- | --- | --- |
| 1 | Section-mean / centerline pressure + dynamic head | new `sample_section_mean_pressure.py`; reconstruct p_rgh/U/rho, sample cut-planes | in progress |
| 2 | Honest convergence metrics | `assess_time_convergence.py` built + RUN on Salt Jin | DONE |
| 3 | Per-case closure re-presentation (Jin/Kirst split + Re guard) | `represent_closures_per_case.py` built + RUN | DONE |
| 4 | Measured-geometry friction (4A/P) | fold real cut-plane area into #1; proposed patch for owner | pending |
| 5 | Mesh-independence study | protocol doc + sbatch; needs runtime/BC | pending |
| 6 | Freeze-window / runtime provenance reconciliation | `reconcile_freeze_windows.py` built + RUN | DONE |
| 7 | Locked CFD↔1D segment map | doc + `validate_segment_map.py` | pending |
| 8 | Smaller corrections (signed τ_w, ρu·cp bulk T, qr doc, NaN handling, stale surfaces) | standalone implementations + documented proposed patch | pending |

## Key results so far (honest, quantified)

- **#2 convergence**: Salt 2/3/4 Jin are stationary in both loop mdot AND gross
  wall duty (<0.2% drift; heat closes to <0.2% of ~500-620 W gross). Salt 1 Jin
  is quasi-stationary (1.3% mdot drift, -2.4% heat closure) — quantifies its
  documented short-window caveat. IMPORTANT: `total_Q` is a near-zero NET
  imbalance; normalizing by gross duty (not its own mean) is essential, else it
  falsely reads as 18-54% "drift".
- **#3 closures**: apparent Darcy f is 3.5-4x (leg) and ~70x (test section) the
  fully-developed laminar 64/Re -> it is form/entrance-loss dominated, NOT wall
  friction. ~20% of apparent Re spread is pure Jin-vs-Kirst property choice.
  Fits have dof<=1 (R^2=1.000 is an overfit artifact, flagged). Pr is perfectly
  collinear with Re (corr -1.00) -> Nu(Re,Pr) is NOT identifiable from this data.
- **#6 provenance**: 13/14 freeze-window lanes are UNVERIFIABLE from staged data
  (continuation/hiq/loq times exceed on-disk by 450-6000 s). Only the earlier
  original window is reconstructable here; the published-closure basis is not.

## COMPLETE — awaiting review (2026-06-30)

Delivered (all reproducible; see `operational_notes/06-26/30/2026-06-30_cfd_1d_closure_workflow.md`):

- Env + reconstruction workflow: `tools/ofenv/of12_env.sh` (v12 reads v13);
  non-mutating zone-free scratch reconstruction recipe (verified on salt2_jin).
- #2 `assess_time_convergence.py` — built + run (Salt 2/3/4 stationary; Salt 1
  quasi). Fixed the misleading total_Q normalization (gross-duty basis).
- #3 `represent_closures_per_case.py` — built + run (form-loss-dominated f;
  dof<=1 overfit flags; Pr collinear with Re).
- #6 `reconcile_freeze_windows.py` — built + run (13/14 lanes unverifiable).
- #7 `validate_segment_map.py` + segment-map note — built + run (only `upcomer`
  ambiguous; `heated_incline` provisional).
- #1 `sample_section_mean_pressure.py` — built + run (section-mean static p_rgh +
  measured bore). Dynamic-head term flagged NOT-yet-trusted (velocity flux
  anomaly) — top open issue.
- #4 measured bore ~2.2x circle (proposed patch). #5 mesh-independence protocol.
  #8 proposed extractor patches (workflow note §4).
- Tests: `tools/analyze/test_claude_action_items.py` — 9/9 pass.
- Report: `reports/2026-06/2026-06-30/2026-06-30_claude_action_items_summary/README.md`.
- Manifest: `imports/2026-06-30_claude_action_items.json`.

## Segment map: LOCKED (owner-confirmed 2026-06-30)

- `heated_incline` = `lower_leg` (heated bottom leg); NOT `left_lower_leg`.
- `upcomer` = `left_lower_leg` + `test_section_span` + `left_upper_leg` (riser).
- `downcomer` = `right_leg`.
Validator now reports 0 unresolved / 0 provisional. No open questions remain.

## Top open technical issue

#1 dynamic head: areaAverage(U)·n ≈ 0 at clean cross-sections (mass flux ~100x
below mdot monitor). Cross-check with faceZone areaNormalIntegrate / flowRatePatch
and re-run on staged continuation (mainline) data.

## PHASE 2 (2026-06-30) — both follow-ups resolved

**Task 2 (velocity anomaly): ROOT-CAUSED + FIXED.** `cuttingPlane` `bounds` is
ignored by OpenFOAM Foundation, so phase-1 planes averaged two counter-flowing
legs (velocity cancelled; area ~2.2x bore; section p_rgh contaminated). Fix =
single-leg radius masking on the raw `.xy` dump. `sample_section_mean_pressure.py`
rewritten; new `diagnose_section_velocity.py`. Verified: clean stations alignment
0.99-1.00, u_bulk ~0.0135 m/s, dynamic head ~0.18 Pa.

**Task 1 (mainline continuation): LOCATED + VERIFIED.** Continuation field data is
in `jadyn_runs/.../2026-06-18_convergence_and_jin_envelope_wave/runs/` (salt2->7915
etc). `reconcile_freeze_windows.py` extended to search jadyn_runs -> 8/14 lanes
now verifiable (remaining 5 = hiq/loq perturbation). Convergence rerun on the 4
Salt-Jin continuations: Salt 2/3/4 stationary (<0.1% heat closure), Salt 1 -2.08%.
Section-mean (#1) rerun closure-grade on salt2_jin continuation (t=7915),
consistent with warmup. Registry registration deferred to coordinator (staging
plan note). Tests: 11/11 pass.

## PHASE 4 (2026-06-30) — B1 UNLOCKED + thermal closures + upcomer convection cell

- **B1 RESOLVED (strongest unlock):** T reconstructs natively under the locally-
  built **OpenFOAM 13** + **gcc/15.2.0** libstdc++ (GLIBCXX_3.4.32) +
  **libRCWallBC.so**. Reusable env `tools/ofenv/of13_env.sh`. (Under OF12 the
  OF13-compiled BC loads but segfaults — ABI mismatch.) OF13 build was already at
  /work/.../openfoam_runtime_recovery/2026-06-02_openfoam13/source/OpenFOAM-13.
- **Thermal closures EXTRACTED** (mainline continuation, Salt 2/3/4 Jin) via
  `tools/extract/sample_segment_htc_uaprime.py` (Agent-finished: T_wall from T
  boundaryField, q from wallHeatFlux, enthalpy-flux bulk T). Results
  `work_products/2026-06-30_claude_thermal_htc/`:
  lower_leg HTC 252/269/288, UA' 16.6/17.7/18.9, R' 0.060/0.057/0.053;
  upcomer Nu 3.11/4.06/4.99 (brackets laminar 3.66/4.36, monotonic in Re).
- **Friction Re** now computable (Agent: `tools/analyze/salt_properties.py` Jin
  mu(T) + `--auto-mu-jin`): Re≈59/81/111. Caveat: inferred bulk T ~445-490 K low
  for molten salt — verify operating T.
- **Upcomer = REAL convection cell** (user direction): negative f is buoyancy-
  driven recirculation, NOT artifact. New `tools/extract/sample_upcomer_convection_cell.py`
  pairs recirculation metric (backflow 15-33%, falls with Re) with nondim groups
  (Ri≈64-300, Ra~3e5, Re). Model + correlation framework:
  `operational_notes/06-26/30/2026-06-30_upcomer_convection_cell_model.md`. Grounded in
  LitRev ch.14 (LeFrancois Nu(Ra,Pr); Jackson Ri=Gr/Re^2 screen).
- Results README: `reports/2026-06/2026-06-30/2026-06-30_claude_closure_results/`.
- Tests: 62 pass across 5 files. New env helper of13, 3 new tools, 1 new property
  module, 1 GCI tool, 5 notes/reports this day.

## PHASE 3 (2026-06-30) — 1D-model plan + parallel execution

Wrote `.agent/journal/2026-06-30/1d-model-status-and-plan.md` (status + phased
plan + parallel work map L1-L6 for all subsequent agents).

Executed next steps (Lane L1 + authored L3/L5):
- **A1 measured D_h** added to `sample_section_mean_pressure.py` (convex-hull of
  masked cut faces): D_h ≈ 21.8 mm on every clean leg (bore ≈3.7e-4 m^2,
  consistent with geometry; the old "2.2x" was the two-leg artifact).
- **A2** reconstructed salt3_jin (t=7618) + salt4_jin (t=10000) continuations;
  section pressures run on all three Salt-Jin continuations
  (`work_products/2026-06-30_claude_continuation_section_mean/`).
- **A3** new `tools/analyze/derive_segment_friction.py` (+test, 16 pass): apparent
  f per segment from section-mean gradients. CROSS-VALIDATES legacy lower_leg
  f~2.5 (new method gives 2.7-3.1). Honestly flags NEGATIVE f in upper_leg
  (cooler-leg pressure recovery — investigate). Results:
  `work_products/2026-06-30_claude_segment_friction/` (+README).
- **L3** new `tools/extract/sample_segment_htc_uaprime.py` + spec
  `operational_notes/06-26/30/2026-06-30_thermal_extraction_spec.md` (runnable; dry-run
  reports B1 blocker; enthalpy-flux bulk T). Runs once OF13+libRCWallBC.so return.
- **L5** new `tools/analyze/compute_gci.py` (+test, 12 pass): Roache/ASME GCI
  calculator for the mesh-independence study.

Tests: 39 pass (11 + 16 + 12). All authored by 3 parallel agents + main thread.

New/updated files this phase: `tools/analyze/diagnose_section_velocity.py`,
`tools/extract/sample_section_mean_pressure.py` (rewrite),
`tools/analyze/reconcile_freeze_windows.py`, `tools/analyze/assess_time_convergence.py`,
`tools/analyze/test_claude_action_items.py`,
`operational_notes/06-26/30/2026-06-30_mainline_continuation_staging_plan.md`,
`work_products/2026-06-30_claude_{velocity_diagnostic,continuation_convergence,continuation_section_mean,freeze_window_reconcile}/`.
