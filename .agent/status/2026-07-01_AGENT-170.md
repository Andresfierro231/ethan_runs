# AGENT-170 Status

Status: compact dense `1.4 in` local run complete; full dense grid deferred to allocation-scale follow-up.

Completed:
- Confirmed Claude AGENT-168 already ran CFD hydraulic closures, but only at `predictive_airside_ins_1.0in_rad_1`; no `1.4 in` boundary was present.
- Added and tested `tools/analyze/build_rom_model_form_fits_and_1p4_boundary.py`.
- Ran the selected `1.4 in` coupled 1D evaluation with `32` workers on active dev node `c318-008`.
- Populated Salt Jin heat-balance references from July 1 validation `case_metric_summary.csv`.
- Refactored dense fit scoring so each `(candidate, salt)` coupled solve is an independent parallel task.
- Ran compact dense `1.4 in` coupled 1D fit with `64` workers on `c318-008`; approximate wall time about 24 minutes.
- Published report/work products under:
  - `reports/2026-07/2026-07-01/2026-07-01_rom_model_form_fits_and_1p4_boundary/`
  - `work_products/2026-07-01_rom_model_form_fits_and_1p4_boundary/`
  - `imports/2026-07-01_rom_model_form_fits_and_1p4_boundary.json`

Key result:
- Best compact dense Salt 2-4 mdot form is `fit_major_k90_1p4`, mean absolute mdot error `3.103%`, RMSE mdot error `3.594%`, coefficients `major_loss_multiplier=2.1`, `k90=0.0`, `k20=0.0`.
- Nearly tied simpler form is `fit_major_defaultK_1p4`, mean absolute mdot error `3.145%`, coefficients `major_loss_multiplier=2.1`, `k90=1.0`, `k20=0.1`.
- Default `1.4 in` rad-on boundary is worse than the old `1.0 in` default: `35.393%` vs `27.367%` mean absolute mdot error.
- Claude casewise CFD closures replayed at `1.4 in` give `13.915%` mean absolute mdot error.
- Heat-balance error is now populated for Salt 2-4 Jin; fitted compact forms are about `14%` mean energy error of heater.

Limitations:
- Compact dense fit is mdot-objective only. It should not be treated as a final predictive ROM closure without pressure-distribution, thermal, and validity penalties.
- Best two-parameter compact result uses `k90=0`; that is a model-form diagnostic, not evidence that physical bend losses vanish.
- Full dense grid still needs a real allocation; compact took about 24 minutes with 64 workers, so full should be scheduled deliberately.

Verification:
- `python3 -m pytest tools/analyze/test_rom_model_form_fits_and_1p4_boundary.py -q` -> `5 passed`
- `python3 -m py_compile tools/analyze/build_rom_model_form_fits_and_1p4_boundary.py` -> passed
