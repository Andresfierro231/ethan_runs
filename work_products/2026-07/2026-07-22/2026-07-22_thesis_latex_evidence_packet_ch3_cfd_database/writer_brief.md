# Writer Brief: Ch3 CFD Database

Use this packet to write the CFD evidence database and extraction-method
background. The safe claim is that registered Salt-family CFD postprocessing
outputs provide a documented reference database for model-form diagnostics and
thesis evidence routing.

Do not call the CFD database an experimental validation set. Do not use
observed CFD fields as predictive runtime inputs unless a later row explicitly
releases that field for a named candidate.

Before using `case_provenance_table.csv` for fit/admission language, resolve
`source_property_gate_todo.csv`. The current gate warning identifies four
fit/model-selection candidate rows that need source-property labels or an
explicit diagnostic/no-fit downgrade.
