# Publication Delta Note

The comparison review does not change the July 21 pressure-corner publication
freeze. All three `corner_lower_right` rows remain `section_effective` pressure
residual / pressure-recovery diagnostics.

Later evidence strengthened the reason for the frozen label:

- The gross static pressure rise is hydrostatic dominated in all three rows.
- The corrected available residual remains negative and must not be clipped into
  a positive loss.
- The endpoint flow is materially recirculating, so a single-stream dynamic-head
  `K` basis is not admissible.
- Same-QOI mesh/time uncertainty and same-basis straight/developing references
  remain missing.
- Geometry/source-envelope evidence still lacks bend radius, part data,
  recovery length, and ordinary low-reverse anchor support.

Publication wording can say the current result is a section-effective
recirculating pressure-residual finding. It cannot say that a component `K`, a
cluster `K`, F6, a clipped negative `K`, or a hidden global multiplier has been
admitted.
