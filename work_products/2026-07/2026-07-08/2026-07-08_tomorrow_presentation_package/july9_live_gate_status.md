# July 9 Live Gate Status Addendum

Generated by: `AGENT-225`
Checked at: `2026-07-09 07:41:25 CDT`

This addendum updates the July 8 presentation package with live scheduler and
gate state. It does not admit new CFD rows and does not change any July 8
figures or closure claims.

## Scheduler Snapshot

Source commands:

- `squeue -j 3275448,3275449,3275560,3280969,3282992,3285548 -o '%i|%j|%T|%P|%M|%l|%D|%R'`
- `sacct -j 3275448,3275449,3275560,3280969,3282992 --format=JobIDRaw,JobID,JobName%34,State,ExitCode,Submit,Start,End,Elapsed,Timelimit,NNodes,AllocCPUS -P`

Machine-readable table:

- `tables/july9_scheduler_snapshot.csv`

Observed:

- Corrected Salt parent jobs are still running:
  - `3275448` `corr_saltq_g1`, elapsed `4-21:14:38` at check.
  - `3275449` `corr_saltq_g2`, elapsed `4-21:04:50` at check.
  - `3275560` `corr_saltq_salt4_all`, elapsed `4-19:19:35` at check.
- Corrected Salt gate job `3280969` is still `PENDING` with reason
  `Dependency`.
- Salt1 nominal continuation `3282992` is running, elapsed `18:19:43` at check.
- Current interactive allocation is `3285548`.

Accounting details:

- `3275448.1` completed successfully on `2026-07-04T23:53:22`, but the parent
  job remains running because other packed steps are still active.
- All visible still-running corrected-Salt steps have `ExitCode 0:0` so far in
  accounting, but this is not a gate verdict.
- The gate job has not started, so no
  `tmp/2026-07-06_overnight_postprocess_jobs/slurm-saltq_gate_after-3280969.*`
  logs exist yet.

Expected timing if the jobs run to walltime:

- `3275448`: time limit reaches around `2026-07-09 10:26:47 CDT`.
- `3275449`: time limit reaches around `2026-07-09 10:36:35 CDT`.
- `3275560`: time limit reaches around `2026-07-09 12:21:50 CDT`.
- `3280969` should become eligible only after the dependency on all three
  corrected-Salt parent jobs clears.

## Log And Preflight Spot Check

Observed:

- `14` corrected-Salt `log.foamRun_corrected_q` files exist under the July 4
  corrected perturbation campaign.
- `18` preflight audit CSV files exist because some Salt4 rows carry both
  canceled/superseded and replacement-job preflight audits.
- A sampled preflight audit for `salt2_jin_lo10q_corrected` reports
  `root_patch_ok=True` and `processor_patch_ok=True`.
- A targeted fatal-marker scan for `FOAM FATAL`, `Fatal error`, `FOAM exiting`,
  `Segmentation`, and `floating point` returned no matches.
- Sampled live log tails for `salt2_jin_lo10q_corrected` and
  `salt4_jin_hi10q_corrected` showed continuing solver progress with current
  timestep/residual output.

Interpretation:

- The corrected-Salt campaign is still live and appears to be progressing, but
  the presentation must still label it as status-only.
- No corrected-Salt row should be added to closure, model-form, F5/F6, or
  T13-validation claims until `3280969` runs and emits a formal gate verdict.

## Slide 11 Update

Use this wording on Slide 11 instead of implying the gate has landed:

- Corrected Salt Q parents are still running and close to their 5-day limits.
- The dependent gate `3280969` is queued and has not fired yet.
- Current status supports the workflow story: the requalification machinery is
  staged, but corrected-Q rows remain outside the fit set.
- Salt1 nominal continuation is running separately and remains non-admitted.

Do not say:

- "Corrected Salt passed."
- "Q perturbations validate the F5/F6 forms."
- "Salt1 is available for closure fitting."
- "T13 can be launched from admitted perturbation evidence."

Acceptable phrasing:

- "Corrected Salt is the near-term range-expansion path. As of this morning,
  the parent jobs are still running and the gate is dependency-pending, so the
  July 8 deck should keep these rows as status-only."

## Immediate Next Check

Run after the expected walltime window:

```bash
squeue -j 3275448,3275449,3275560,3280969,3282992 -o "%i|%j|%T|%P|%M|%l|%D|%R"
sacct -j 3275448,3275449,3275560,3280969 --format=JobID,JobName%30,State,ExitCode,Elapsed,Start,End -P
tail -80 tmp/2026-07-06_overnight_postprocess_jobs/slurm-saltq_gate_after-3280969.out
tail -80 tmp/2026-07-06_overnight_postprocess_jobs/slurm-saltq_gate_after-3280969.err
```
