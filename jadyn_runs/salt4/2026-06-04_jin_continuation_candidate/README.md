# Salt 4 Jin Continuation Candidate

This directory stages a writable continuation of the staged Salt 4 Jin case for a capped 72-hour extension run.

- Source case remains unchanged.
- The continuation copy is created under `case_stage/`.
- Submit with `sbatch --parsable run_continuation_openfoam13.sbatch`.
