# TODO

- Re-run the exact readability audit after the external `chmod` pass completes.
- Promote fully readable cases from inventory-only status into per-case
  registration and import manifests.
- Stage the salt sweep into local writable analysis folders without altering the
  source tree.
- Build per-case setup dossiers for the staged water cases once boundary and
  log access is stable.
- Add extraction status fields after the first local QoI pass.
