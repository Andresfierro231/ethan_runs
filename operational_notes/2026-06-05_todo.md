# 2026-06-05 TODO

## Priority 1: repaired continuation retries
- Monitor `3202708` and the new Salt 4 Jin retry `3210231`.
- Finish staging the Salt 1 Kirst continuation candidate under `jadyn_runs/salt1/2026-06-05_targeted_campaign/`.
- Submit Salt 1 Jin and Salt 1 Kirst targeted continuation retries after the staging copy completes.
- If the repaired `WM_MPLIB=INTELMPI` bootstrap still fails, move to an interactive compute-node diagnosis and inspect the loaded OpenFOAM/MPI libraries directly.

## Priority 2: report refresh triggers
- If Salt 4 or Salt 1 produce new runtime, refresh:
  - `reports/2026-06-05_ethan_continuation_diagnosis/`
  - `reports/2026-06-05_ethan_convergence_and_salt1_campaign/`
  - `reports/2026-06-05_ethan_wall_loss_resistance_coupling/`
- If Salt 2 continuation writes a new defensible checkpoint, refresh the June 4 and June 5 transient/behavior/validation packages.

## Priority 3: hydraulic and thermal clarification work
- Build transient `p_rgh` history extraction for the upper leg, left leg, and test-section branch.
- Use the new pressure-history path to test whether the current latest-time resistance ranking is stable in time.
- Keep wall-loss sensitivity ahead of insulation-thickening for the next alternate salt runs.

## Priority 4: convergence and manuscript discipline
- Keep the eight-case convergence framing explicit: coded convergence and practical steadiness are separate questions.
- Keep `TW10` excluded from RMSE scorecards.
- Treat the water laminar rows as `borderline_but_usable` rather than fully converged unless a later audit improves the tail-drift evidence.

## Priority 5: ParaView temperature-slice rollout
- Finish the all-case latest-time T reconstruction pass into staging/render_inputs reconstructed_case mirrors.
- Fan out one pvbatch render per registered case against the reconstructed mirrors and record PNG existence per case.
- Treat file existence, not raw pvbatch exit code, as the render success criterion until the ParaView post-output nonzero-exit behavior is understood.
