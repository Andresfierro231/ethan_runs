# TODO — 2026-06-01

## Salt2 Continuation Runtime Recovery
- Treat job `3200407` as resolved root-cause history: it failed because the container wrapper used the broken `bash -lc` pattern and every rank wrote `foamRun: command not found`.
- Treat job `3200937` as the current continuation blocker: the wrapper now uses a non-login shell, but the container runtime still exits before exposing a usable `foamRun` command.
- Treat probe job `3200970` as evidence that the original OpenFOAM 13 bashrc path from the source log is not readable from the current compute-side environment.
- Locate a readable OpenFOAM 13 runtime or a container image that exposes `foamRun` plus a compatible custom-library path for `libRCWallBC.so`.
- Keep the imported source tree immutable and perform any further restart work only in `jadyn_runs/salt2/2026-06-01_continuation_candidate/`.
- Preserve processor-decomposed restart provenance; do not reconstruct or re-mesh unless the runtime path is explicitly redesigned and documented.

## Salt2 Analysis
- Do not re-run QoI extraction for `val_salt_test_2_coarse_mesh_laminar` until a successful continuation checkpoint is actually written.
- Keep the low heat-loss discrepancy explicitly marked unresolved.
- After a successful continuation chunk, re-check mdot stability, total wall heat trend, TP/TW temperatures, PIV-slab diagnostics, and the coded convergence gap before any publish-facing reinterpretation.

## Modern Runs First Batch
- The first staged batch is now extracted locally: 8 salt viscosity-screen cases plus 4 water laminar cases.
- Use `work_products/campaigns/2026-06-01_modern_runs_first_batch/` as the local campaign summary root for this pass.
- Keep the 4 water `kOmegaSSTLM` cases in setup-only status until actual solver-output trees exist.
- Review the two incomplete Kirst salt cases separately before treating the salt property screen as a fully comparable quartet.
- Add a variant-aware reference/join strategy before trying to publish Jin/Kirst salt comparisons into `cross_model_comparison`.

## Reporting And Publication
- Publish handoff is deferred for `salt_test_2` until continuation succeeds or a defensible no-continuation decision is documented.
- Publish handoff is deferred for the modern first batch until comparison contract rules for Jin/Kirst variants are defined and the incomplete runs are dispositioned.
- Keep dated journal updates synchronized with runtime-state changes, extraction-state changes, and any future publish-gate decisions.
