# 2026-06-07 TODO

## Priority 1: continuation closeout after the repaired-runtime refresh
- Monitor `3210231` for Salt 4 Jin and record the final state in the continuation diagnosis once it stops running.
- Decide whether Salt 2 continuation `3202708` deserves another walltime extension now that the recorded outcome is `TIMEOUT`, not an environment/bootstrap failure.
- Repair the Salt 1 Kirst staged restart tree before any retry after `3211199`; the missing root-level `T` must be fixed in staging first.

## Priority 2: finish the temperature-slice rollout under the new convention
- Use `tools/extract/render_last_timestep_temperature_slices.py` only with the `z`-reference frame now: reference `z` plane, `slice_normal = [0, 0, 1]`, camera from `-z`.
- Fan out one render job per remaining registered case so `figures/png`, `figures/svg`, and `figures/pdf` are populated beyond the two verified smoke cases.
- Keep `figures/last_timestep_temperature_slice_status.json` as the aggregate machine-readable status file and fold in later case-level renders there.

## Priority 3: transient axial rebuild blocker
- Repair the reconstructed-`T` readability problem that is causing the scalar-token parse error in `foamPostProcess` during the transient/axial base-package rebuild.
- Re-run `tools/analyze/build_ethan_transient_axial_package.py` cleanly after that fix so the package outputs reflect the typed figure convention without relying on partial earlier products.

## Priority 4: reporting discipline
- Keep the typed figure convention everywhere going forward: `figures/png`, `figures/svg`, and `figures/pdf`.
- Do not reintroduce flat root-level generated figure files into report package roots.
- Keep June 7 follow-on notes separated into observed outputs, interpretation, contradictions/caveats, and next suggested actions.
