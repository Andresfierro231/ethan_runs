# 2026-06-17 TODO

## Priority 0: produce one rigorous methods note for the new transport variables

- Write one source-of-truth math note that reconciles the June 10 Salt 2 methodology companion, the implemented extractor code, and the June 15 Salt-family field-transport outputs.
- The note should be written so a later paper/report author does not have to reverse-engineer formulas from code.
- Treat this as a methods-and-interpretation note, not just a formula dump.

## Priority 1: exact variable families that must be covered

- Shear-based hydraulic quantities:
  - projected wall shear along the repaired streamwise tangent
  - shear-based Darcy friction factor
  - shear-based Fanning friction coefficient
  - shear-implied major pressure gradient
- Direct wall-registered pressure quantities:
  - wall-area-averaged `p_rgh`
  - wall-area-averaged `p`
  - finite-difference streamwise gradients
  - direct Darcy / Fanning reductions from wall-registered gradients
- Effective thermal quantities:
  - `q''_wall`
  - `q'_wall`
  - effective `HTC(x)`
  - effective `UA'(x)`
  - effective thermal resistance `R'_th(x) = 1 / UA'(x)`
- Momentum resistance quantities:
  - estimated momentum resistance from shear-implied `dp/ds`
  - direct momentum resistance from wall-registered `dp/ds`

## Priority 2: interpretation boundaries that must be explicit

- State clearly that the direct hydraulic quantities are wall-registered reductions, not centerline probe pressures.
- State clearly that `HTC`, `UA'`, and `R'_th` are effective transport indicators built from wall heat flux plus cut-plane bulk support, not intrinsic closure-quality coefficients.
- Explain exactly why the effective thermal quantities can blow up:
  - `|T_bulk - T_wall|` becomes small
  - cut-plane support may be poor or disconnected
  - sign changes and weak local thermal driving force make ratio metrics unstable
- State clearly that momentum resistance is a local proxy `dp/ds / mdot`, not yet a canonical publication-ready nondimensional group.

## Priority 3: formulas and implementation details that must be pinned down

- Define every numerator and denominator explicitly, including units.
- Record the flow-direction sign convention and how wall-pressure gradients are aligned to “pressure drop along flow”.
- Record the geometry surrogates:
  - local flow area
  - local wetted perimeter
  - local hydraulic diameter
- Record the bulk support path:
  - connected-region or chosen-region cut-plane bulk temperature
  - mass-flux weighting
  - retained-window averaging assumptions
- Record the exact QC/masking gates for thermal support, including the `|T_bulk - T_wall|` floor and area-ratio logic.
- Record the direct-gradient differencing rule and any end-point handling.

## Priority 4: exact sources to reconcile

- `tools/extract/sample_leg_centerline_major_loss.py`
- `reports/2026-06-10_ethan_salt2_case_analysis_package/METHODOLOGY_MATH_COMPANION.md`
- `reports/2026-06-10_ethan_salt2_case_analysis_package/README.md`
- `journals/2026-06/2026-06-10_ethan_runs.md`
- `reports/2026-06-15_ethan_representative_transport_comparison/README.md`
- `reports/2026-06-15_ethan_field_transport_campaign/README.md`

## Priority 5: deliverables

- A rigorous standalone math/methods note in a durable report or operational-note location.
- A short companion interpretation note that says what claims are safe and what claims are not safe.
- If the math note reveals ambiguity or inconsistency, file a follow-up TODO before touching the package narrative.

## Priority 6: paper-facing branch transport integration

- Promote the branch-thermal results into the paper/report layer only after the
  internal interpretation package and the upgraded representative/campaign
  builders are review-clean.
- Use these as the canonical paper-facing branch sources:
  - `reports/2026-06-15_ethan_representative_transport_comparison/representative_branch_thermal_summary.csv`
  - `reports/2026-06-15_ethan_field_transport_campaign/field_transport_branch_thermal_comparison.csv`
  - `reports/2026-06-15_ethan_all_runs_field_transport_campaign/field_transport_branch_thermal_comparison.csv`
- Add branch QC figures alongside branch thermal figures when a paper figure
  risks hiding masked or weak-support regions.

## Priority 7: coordination and status cleanup

- Clean stale AGENT-072 task text so it no longer refers to pending water jobs
  or pre-branch-schema campaign state.
- Check related README / status language for any remaining references to the
  old all-runs campaign schema that lacked branch thermal and QC figures.
- If any downstream note still points first to the June 10 Salt 2 methodology
  companion, update it to point to the June 17 math reference instead.

## Priority 8: paper-task promotion gate

- Open a separate `../papers` task before touching `3d_analysis`, because the
  current scrutiny pass lives only in `ethan_runs`.
- Use `reports/2026-06-17_ethan_transport_scrutiny_package/paper_safe_asset_map.csv`
  as the promotion gate for any new transport figure or table import.
- Promote only the explicitly safe Salt 2 / Salt-family subsets first:
  - Salt-family heat-loss subsets
  - Salt-family azimuthal subsets with circ-mean caveat
  - Salt 2 hydraulic mechanism figure with redistribution-only narration
  - Salt 2 / Salt-family branch-thermal subsets filtered to
    `left_lower_leg`, `test_section_span`, `left_upper_leg`, and `upcomer`

## Priority 9: hydraulic contradiction follow-up

- Investigate the two current `requires_code_fix = yes` rows from
  `reports/2026-06-17_ethan_transport_scrutiny_package/transport_contradiction_log.csv`:
  - `val_water_test_1_coarse_mesh_laminar / left_lower_leg`
  - `val_water_test_2_coarse_mesh_laminar / left_lower_leg`
- Do not strengthen cross-family hydraulic interpretation until those
  left-lower-leg direct-vs-shear disagreements are explained as either real
  physics limits or reduction-method artifacts.

## Priority 10: use the June 18 Salt handoff package for any paper promotion

- Open a separate `../papers` task before touching `3d_analysis`.
- Use only:
  - `reports/2026-06-18_ethan_salt_paper_handoff_package/staged_assets_manifest.csv`
  - `reports/2026-06-18_ethan_salt_paper_handoff_package/figure_claim_map.csv`
  - `reports/2026-06-18_ethan_salt_paper_handoff_package/table_claim_map.csv`
  as the promotion surface.
- Keep the staged figure/table set aligned to the current section map:
  - `04_salt_family_results`
  - `05_salt2_mechanism_results`
  - appendix/support only for trust and QC figures

## Priority 11: keep the June 18 all-runs analysis package as the interpretive authority

- Use `reports/2026-06-18_ethan_transport_analysis_package/case_family_interpretation_matrix.csv`
  for all-runs claim classification before broadening any narrative.
- Use `reports/2026-06-18_ethan_transport_analysis_package/transport_contradiction_priority.csv`
  as the contradiction queue for future hydraulic follow-up.
- Do not bypass the June 18 analysis package when making new cross-family claim
  decisions.

## Priority 12: use the focused June 18 scientific interpretation package as the conclusion-writing authority

- Treat `reports/2026-06-18_ethan_transport_scientific_interpretation_package/`
  as the current source of truth for:
  - contradiction resolution status
  - branch-by-branch thermal interpretability
  - scientific conclusions
  - model dependency readiness
  - remaining blockers
- Use:
  - `contradiction_resolution_rows.csv`
  - `branch_thermal_interpretation.csv`
  - `scientific_conclusions.csv`
  - `cross_family_claims_audit.csv`
  - `internal_only_decisions.csv`
  before drafting any new model-dependency or paper-facing narrative.

## Priority 13: exact next hydraulic blocker

- Water 1 `left_lower_leg` is now a resolved exclusion, not a candidate for
  stronger use:
  - weak direct `p_rgh` branch-mean sign cancellation
  - branch-end cumulative direct drop remains positive
- Water 2 `left_lower_leg` has now also been resolved as an exclusion in the
  closure package:
  - one retained time carries the only non-modal alignment sign
  - branch-end cumulative direct drop remains positive in every retained time
  - remaining branch-mean sign changes are weak-signal cancellation artifacts
  - do not make stronger cross-family hydraulic claims on `left_lower_leg`
    because the branch is excluded from the usable Water evidence subset

## Priority 14: use the June 18 interpretation closure package as the final current authority

- Treat `reports/2026-06-18_ethan_transport_interpretation_closure/` as the
  final current authority for:
  - Water 2 left-lower-leg hydraulic closure status
  - contradiction resolution rows
  - cross-family claim blocking status
  - downstream model-dependency readiness
- Use:
  - `water2_left_lower_leg_hydraulic_audit.csv`
  - `water2_left_lower_leg_hydraulic_audit.md`
  - `contradiction_resolution_rows.csv`
  - `cross_family_claims_audit.csv`
  - `model_dependency_readiness.md`
  before drafting any new cross-family hydraulic narrative.

## Priority 15: use the June 18 Water hydraulic evidence subset package for Water-only branch pressure work

- Treat `reports/2026-06-18_ethan_water_hydraulic_evidence_subset/` as the
  current Water-only branch-pressure screen.
- Current Water hydraulic candidates:
  - `right_leg`
  - `test_section_span`
  - `upper_leg`
- Current Water contextual-only branches:
  - `lower_leg`
  - `left_upper_leg`
- Current Water excluded branch:
  - `left_lower_leg`
- Do not promote any Water hydraulic branch directly to cross-family readiness
  from this package alone.

## Priority 16: use the June 18 Salt hydraulic evidence subset package as the parallel Salt-only branch pressure screen

- Treat `reports/2026-06-18_ethan_salt_hydraulic_evidence_subset/` as the
  current Salt-only branch-pressure screen.
- Current Salt hydraulic candidates:
  - `lower_leg`
  - `right_leg`
  - `left_lower_leg`
  - `test_section_span`
  - `left_upper_leg`
  - `upper_leg`
- Current shared clean Salt/Water overlap:
  - `right_leg`
  - `test_section_span`
  - `upper_leg`
- Do not promote the overlap directly to cross-family readiness from this
  package alone; the closure package still blocks cross-family hydraulics.

## Priority 17: use the refreshed June 18 Salt handoff package as the paper-facing gate

- Treat `reports/2026-06-18_ethan_salt_paper_handoff_package/` as the
  current Salt manuscript staging surface.
- Use `closure_gate_note.md` together with:
  - `staged_assets_manifest.csv`
  - `figure_claim_map.csv`
  - `table_claim_map.csv`
  before any future `../papers` edit.
- The next direct manuscript edit still requires a separate claimed
  `../papers` task.

## Priority 18: use the cross-family redesign screen only as a method-choice gate

- Treat `reports/2026-06-18_ethan_cross_family_hydraulic_redesign_screen/` as
  the current redesign-feasibility authority for shared Salt/Water hydraulic
  branches.
- Current shared clean overlap:
  - `right_leg`
  - `test_section_span`
  - `upper_leg`
- Current redesign conclusions:
  - `right_leg` -> bounded cumulative-direct redesign candidate
  - `test_section_span` -> no clear redesign priority
  - `upper_leg` -> timeseries-only until the summary terminal-direct gap is
    reconciled
- Do not treat this package as cross-family readiness. It is only a guide for
  which direct observable to test first if a future redesign is attempted.

## Priority 19: use the Salt thermal model-surface ranking as the current fit gate

- Treat `reports/2026-06-18_ethan_salt_thermal_model_surface_ranking/` as the
  current Salt-only thermal modeling authority on the defended branch subset.
- Current ranking:
  - primary: `effective_ua_profile`
  - secondary: `effective_htc_profile`
  - supporting-only: `branch_average_thermal_resistance`
  - diagnostic-only: `thermal_resistance_profile`
- Keep the branch gate unchanged:
  - `left_lower_leg`
  - `test_section_span`
  - `left_upper_leg`
  - `upcomer`
- Do not promote `right_leg` thermal behavior from this ranking package.

## Priority 20: use the pressure / feature / Water support note to preserve the remaining exclusions

- Treat `reports/2026-06-18_ethan_pressure_feature_support_note/` as the
  current compact follow-on note for the June 17 pressure / HTC package.
- Current guarded conclusions:
  - feature `K_eff` -> `not_ready`
  - Water `test_section_span` -> `supporting_only`
  - hydro-corrected straight-section pressure rows -> `diagnostic_only`
- Do not widen those readiness states without a new bounded methods package.

## Stop Conditions

- Do not stop after only restating formulas from June 10.
- The note must also explain the newer June 15-era variables and the interpretation limits that are currently scattered across journals, READMEs, and code comments.
- If tomorrow's work changes any formula wording or sign-convention wording in an authoritative report, update the affected provenance note and journal entry the same day.

## Priority 21: monitor the submitted June 18 continuation wave

- Active continuation jobs:
  - `3244955` Salt 1 Jin `120 h`
  - `3244951` Salt 2 Jin `120 h`
  - `3244950` Salt 3 Jin `120 h`
  - `3244954` Salt 4 Jin `120 h`
  - `3244953` Water 1 `72 h`
  - `3244957` Water 2 `72 h`
  - `3244952` Water 3 `72 h`
  - `3244956` Water 4 `72 h`
- Use `jadyn_runs/modern_runs/2026-06-18_convergence_and_jin_envelope_wave/campaign_manifest.csv`
  as the source of truth for paths and job IDs.
- Day 1:
  - verify all eight jobs leave `PD` cleanly and begin writing to their staged case roots
- Day 2:
  - check first clean new writes
  - keep the Jin-only Salt DOE children gated until at least one clean parent write exists
- Day 3:
  - first convergence checkpoint on any run with enough new late-time writes
- Day 4-5:
  - decide which continued runs are eligible for package rebuild reuse
  - if parent continuation health is good, stage the two Jin-only Salt envelope children per Salt case
