# TODO - 2026-06-04

- If new `val_salt_test_2` continuation probe outputs appear beyond `1724 s`, rerun `tools/analyze/build_ethan_report_package.py` and `tools/analyze/build_salt2_behavior_package.py` to refresh the Salt 2 behavior package.
- Extend the Salt 2 behavior package with section-wise drift ranking and an explicit steady-window classification per metric.
- Decide whether to add a stitched continuation-probe recovery path if missing post-1724 TP/TW/mdot outputs can be reconstructed from future solver outputs.
- Draft follow-on Salt 2 alternates using the adopted defaults: hotter start, `1.65 in` insulation, slightly lower cooler `h`, and Jin-style viscosity.
- Keep `TW10` excluded from RMSE-based Salt 2 scorecards unless a later audit explicitly restores it.
- Fold the residual-based essential-steadiness classification into the next broader steady-window audit so coded convergence and practical usability are reported separately.
- Refresh the all-salt behavior package after Salt 4 Jin job `3208600` produces new postProcessing outputs.
- Implement a non-destructive `p_rgh` section pressure-drop extraction pass for the manuscript hydraulic analysis.
- Implement axial thermal reductions by section for `q_w(x)`, `T_w(x)`, `T_bulk(x)`, and derived `Nu(x)` / `h(x)` where existing outputs support them.
- Continue expanding the Ethan CFD manuscript dossier in `../papers/dmdc_analysis/` using the June 4 all-salt package as the current source of run-state claims.
- The section pressure-drop extraction pass is now implemented in `tools/analyze/build_ethan_section_transport_package.py`; next step is extending it to transient `p_rgh` histories instead of latest-time-only reductions.
- The first axial `h(x)` / `Nu(x)` pass is still blocked on a cleaner reconstructed `T`-field workflow or a compute-node sampling path.
- Salt 4 Jin background continuation job `3208600` failed on dummy-Pstream parallel startup and job `3208837` failed on a `libstdc++` / `GLIBCXX_3.4.32` runtime mismatch; troubleshoot the continuation runtime environment before any further Salt 4 resubmission.
- Refresh `reports/2026-06-04_ethan_section_transport_package/` after any future Salt 4 Jin continuation succeeds or after transient pressure-history extraction is added.

## Added Later Same-Day Update 6
- Monitor Slurm job `3208905` for Salt 4 Jin continuation and confirm whether the env-script bootstrap resolves the previous `GLIBCXX` runtime failure.
- Let `tools/analyze/build_ethan_transient_axial_package.py` finish, then audit the generated axial rows for which cases populated `areaAverage_T_k` / `areaAverage_Nu` and which cases only carried q-based axial products plus `axial_field_extraction_error`.
- Decide whether the manuscript axial discussion should lead with q-profile / patchwise heat-transfer behavior first, with `Nu` only for rows whose reconstructed `T` path proved stable.
