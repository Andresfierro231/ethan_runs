# 2026-06-10 TODO

## Priority 0: reopen the right context first

- Read `journals/2026-06/2026-06-10_ethan_runs.md` before making new claims or changing the thermal method.
- Reopen `reports/2026-06-10_ethan_salt2_case_analysis_package/README.md` and `summary.json` to confirm the current frozen hydraulic window is still `7483-7487 s`.
- Reopen the two key thermal figures first:
  - `figures/png/case_loopwise_thermal_profiles.png`
  - `figures/png/case_loopwise_thermal_support_qc.png`

## Priority 1: thermal review boundary

- Review whether the remaining `285` thermal-support-flagged retained rows are acceptable if they remain masked from the primary local HTC / `UA'` curves.
- Check whether the current package wording is conservative enough:
  - mass-flux-weighted
  - connected-region selected
  - support-filtered
  - effective, not intrinsic
- Confirm whether the `flow_direction_sign_hint = +1.0` assumption should stay as a documented profile assumption or be replaced by a stronger direction-inference method.

## Priority 2: if another thermal refinement is needed

- Start from `raw_extraction/bulk_cross_section_temperature_samples.csv` and `case_loopwise_thermal_support_qc.png`, not from the final figures alone.
- Focus first on the remaining flagged `lower_leg` and `right_leg` zones where chosen-region area ratio still exceeds `2.0`.
- Do not relax masking thresholds first. Try to improve support selection before weakening the report-safety gates.

## Priority 3: package refresh commands

- If no extractor logic changes are made, refresh the package from the existing raw extraction:
  - `python tools/analyze/build_ethan_case_analysis_package.py --source-id val_salt_test_2_coarse_mesh_laminar --raw-extraction-dir reports/2026-06-10_ethan_salt2_case_analysis_package/raw_extraction`
- If extractor logic changes are made, rerun the full package build:
  - `python tools/analyze/build_ethan_case_analysis_package.py --source-id val_salt_test_2_coarse_mesh_laminar`
- After either path, verify that these three artifacts still agree exactly:
  - `summary.json`
  - `raw_extraction/leg_major_loss_extraction_summary.json`
  - `raw_extraction/thermal_sanitization_summary.json`

## Priority 4: report-writing angle

- Keep the heat narrative anchored to the sectionwise wallHeatFlux balance and net closure, not to local HTC first.
- Use the loopwise thermal profiles for the main transfer story and the thermal-support QC figure as the honesty/limitations panel.
- Treat local effective HTC / `UA'` as derived CFD-side indicators unless a later review explicitly clears stronger wording.

## Priority 5: exact files to inspect tomorrow

- `tools/extract/sample_leg_centerline_major_loss.py`
- `tools/analyze/build_ethan_case_analysis_package.py`
- `tools/case_analysis_profiles.py`
- `reports/2026-06-10_ethan_salt2_case_analysis_package/raw_extraction/bulk_cross_section_temperature_samples.csv`
- `reports/2026-06-10_ethan_salt2_case_analysis_package/raw_extraction/thermal_sanitization_summary.json`
- `reports/2026-06-10_ethan_salt2_case_analysis_package/major_loss_summary.csv`
- `reports/2026-06-10_ethan_salt2_case_analysis_package/major_loss_cumulative_timeseries.csv`

## Stop Conditions

- If tomorrow’s work is only review/report framing, do not rebuild the package unless a real discrepancy is found.
- If another method change is made, do not stop before updating:
  - today’s journal
  - package README
  - raw/provenance summaries
  - this dated TODO note
