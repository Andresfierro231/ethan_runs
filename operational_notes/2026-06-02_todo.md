# TODO — 2026-06-02

## OpenFOAM 13 Runtime Recovery
- Stable canonical runtime path is `/work/09748/andresfierro231/ls6/OpenFOAM-13`.
- That alias currently points to the dated fallback build root `/work/09748/andresfierro231/ls6/openfoam_runtime_recovery/2026-06-02_openfoam13` for provenance.
- Build `3202515` and probe `3202516` both completed successfully.
- Probe `3202516` verified compute-side `foamRun -help` and readable `libRCWallBC.so`.
- If coworker access later provides the original private runtime `/work/09807/ethanrozak/ls6/OpenFOAM_V13/OpenFOAM-13`, compare it against the fallback tree before repointing the canonical alias.

## Salt2 Continuation Gate
- Runtime gate is now cleared; active continuation job is `3202708`.
- Treat `3202687` as obsolete wrapper-history only; it failed because the launched ranks did not resolve `foamRun` and Intel MPI lacked PMI wiring.
- Current wrapper now uses the resolved `foamRun` path, `srun --mpi=pmi2`, and `I_MPI_PMI_LIBRARY=/usr/lib64/libpmi2.so`.
- Next checkpoint is not just scheduler survival; confirm actual timestep progression and a clean restart write before calling the continuation path fully recovered.

## Modern Runs First Batch
- The variant-aware publish package now exists in the sibling comparison repo under `campaigns/2026-06-02_ethan_modern_runs_first_batch_v1/`.
- Preserve the rule that Jin and Kirst stay as separate contract rows while sharing their base `salt_test_n` identity.
- Current publish-ready rows are limited to the two converged Kirst cases; all others remain in `convergence_audit_required`.
- Keep the setup-only water `kOmegaSSTLM` cases outside solver-output claims until runtime outputs exist.

## Water Laminar Determination
- Use `reports/2026-06-02_water_laminar_claim_audit/` as the current claim-status summary.
- All four water laminar cases need convergence audit before claim use.
- `water_test_4` should be treated as the weakest of the four because it ended at shorter final time without a recorded convergence marker.
