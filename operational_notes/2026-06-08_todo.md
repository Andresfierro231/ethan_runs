# 2026-06-08 TODO

## Priority 0: sponsor deck handoff
- Use `reports/2026-06-08_sponsor_salt_status_deck/slide_outline.md` as the sponsor-facing salt-first deck scaffold.
- If the final deck will be assembled on the laptop, pull `reports/2026-06-08_sponsor_salt_status_deck/` together with the typed figure bundles via `tools/publish/download_results_to_laptop.sh`.
- If Salt 4 Jin `3210231` stops before the presentation, refresh only the live-status wording rather than rebuilding the full report stack first.

## Priority 1: laptop transfer and local-open verification
- Run `tools/publish/download_results_to_laptop.sh` from the personal laptop to pull the current report packages, typed figure folders, and selected reconstructed cases.
- After download, verify that the reconstructed-case mirrors open locally in ParaView on the laptop and that the typed `png/svg/pdf` figure bundles are intact.
- If the first transfer is too large, trim the pull to the immediate cases of interest rather than falling back to symlinked or non-provenance paths.

## Priority 2: remaining continuation state
- Check whether Salt 4 Jin `3210231` has finished and update the continuation diagnosis once its final state is known.
- Decide whether Salt 2 continuation `3202708` is worth another extension now that the recorded outcome is `TIMEOUT`, not an environment/bootstrap failure.
- Repair the Salt 1 Kirst staged restart tree before any retry after `3211199`; the missing root-level `T` remains the blocker there.

## Priority 3: rendering and postprocessing follow-up
- Fan out the repaired `z`-reference ParaView render path to the remaining registered cases and merge those results into `figures/last_timestep_temperature_slice_status.json`.
- Repair the reconstructed-`T` readability problem so `tools/analyze/build_ethan_transient_axial_package.py` can be rerun cleanly without q-only fallbacks.

## Priority 4: next figures or simulations to consider
- Choose whether to prioritize pressure-evolution figures, geometric-arc-length axial figures, or Jin-vs-Kirst delta figures first.
- Decide whether the next simulation budget should go to a Salt 2 extension, a Salt 1 Kirst repaired restart retry, or a targeted upper-leg / loss sensitivity run.
